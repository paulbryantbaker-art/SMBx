built      2026-08-22 · TEMPLATES — THE VISUAL REGISTER (6pp Letter), OXBLOOD.
           Regenerated after the palette cutover; the Deal Green edition is
           gone with the rest of the green renders.
builder    scripts/studio/templates-card.py — NOT in rebuild-all.
           It now READS house/tokens.ts live (tsx) instead of carrying a
           transcribed palette. It was hard-coded Deal Green, so after the
           cutover it kept rendering the retired system correctly and
           silently — the exact failure the deck exists to prevent.
sources    Thumbnails point at the OXBLOOD renders: the offer document and
           featured cover of 2026-08-22, and dead-deal-economics/2026-08-22.
           A MISSING FILE IS NOW AN EMPTY PLATE reading AWAITING OXBLOOD
           REBUILD, not a crash — the deck died on the first deleted green
           render, and its own law already said to say there is no render
           rather than draw one.
still owed anything whose BUILD.txt names build-deck.mts needs Chromium and
           therefore the Mac: hvac-2026-read, corp-dev-cost-sheet. Those
           plates read AWAITING OXBLOOD REBUILD until they are re-run.
