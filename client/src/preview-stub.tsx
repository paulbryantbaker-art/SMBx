/** Preview-build stub (see preview.vite.config.mts): the auth-gated shells
 *  are unreachable in the logged-out artifact preview; stubbing them keeps
 *  the single-file bundle small. Not part of the shipped app. */
export default function PreviewStub() {
  return null;
}
