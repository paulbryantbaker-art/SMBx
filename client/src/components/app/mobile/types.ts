/**
 * Local types for the mobile shell.
 *
 * AppShell-level types live in ../types.ts (AppDeal, AppShellInnerProps).
 * This file exports only shapes that are scoped to the mobile surface.
 */

/** The four tabs in the post-rewire mobile app. */
export type MobileTab = 'today' | 'deals' | 'chat' | 'inbox';
