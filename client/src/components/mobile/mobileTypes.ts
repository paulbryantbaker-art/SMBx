/**
 * Shared mobile types — extracted from the (now-deleted) MobileSidebar.
 * Both LearnDrawer and AppShell consume these.
 */

export type LearnDest =
  | 'sell'
  | 'buy'
  | 'raise'
  | 'integrate'
  | 'advisors'
  | 'how-it-works'
  | 'pricing';

export type WorkspaceTool =
  | 'documents'
  | 'library'
  | 'analysis'
  | 'sourcing'
  | 'pipeline';
