CLAUDE.md — smbx.ai
Last updated: 2026-07-11

> **For a current-vs-stale map of the whole repo, read `REPO_STATUS.md` at the root.** Archived docs live in `docs/_archive/` — do not build against them.

## What This Is
**An AI-forward, buy-side corp-dev-as-a-service practice (THE LINE v2, pivoted 2026-07-11).** smbX.ai is no longer a public product — it is the private instrument of the practice: Paul (and team) running buy-side acquisitions for acquirer clients through close, with Yulia + DEFINITIVE doing the work that used to require an analyst team. The app serves the team only (practice mode); the logged-out surface is a one-page practice front door. Inside the app the experience stays chat-first — the practitioner talks to Yulia, and the practitioner carries the deal.

## THE SPLIT (operating model — LAW, Paul 2026-07-31; supersedes "the app is marketing only", 2026-07-27)
**"i think i want all Studio work to be on disc Cowork — ill do CRM and Deal management in the app."**

The 2026-07-27 rule ("marketing only, everything in Cowork") was right about the cost problem and wrong about the scope. It was written the day the org's Anthropic key hit its spend ceiling mid-synthesis and *every* authoring function stopped at once, because the app routes all of them through that one metered key. That diagnosis holds. But it answered the COST question and left the STATEFULNESS question unanswered, and CRM plus deal management is entirely a statefulness problem.

**The line is SHAPE, not cost — documents are files, pipelines are rows.** Full reasoning and the cost figures behind it: **`WHERE_THE_WORK_HAPPENS.md`** (read it before deciding where new work goes).

- **STUDIO IS ON DISK. All of it.** Research masters, market maps, who's who, target maps, theses, deal analysis documents, and every piece of collateral are produced in a Cowork session against `~/Documents/smbx-studio`. The workspace (created by `scripts/studio/init-workspace.mts`) is the system of record: `markets/<m>/{research,master.md,versions,documents,collateral}`, `deals/<d>/{documents,analysis}`, plus `media/ assets/ collateral/ decks/`. It ships its own `CLAUDE.md` (the laws), `PLAYBOOK.md` (the document specs), `FORMATS.md` (containers) and `DESIGN.md` (the look) so a session opened there behaves correctly without the app enforcing anything.
- **CRM AND DEAL MANAGEMENT ARE IN THE APP.** `crm_accounts` / `crm_contacts` / `crm_activity` (migration 113, `server/routes/crm.ts`) is the CLIENT pipeline — the acquirers the practice serves, distinct from the targets it underwrites. `deals` and its ~62 related tables are the deal pipeline. Building here is expected, not a violation: **a CRM is forms and SQL and calls no model at all.** So are `v19ModelRuntime.ts` and the canvas math — verified, no API key reference in either.
- **The repo is the engine, not the product.** `house/` (brand system, `house/audit.ts` the pure citation audit, `house/screen.ts` the target screen, `house/leads.ts` the buy-side lead model) and `scripts/studio/` (`audit.mts`, `build-report.mts`, `build-deck.mts`, `build-onepager.mts`, `build-og-card.mts`, `thesis.mts`, `screen.mts`, `leads.mts`) are what a local session runs. They take no API key. Anything in `house/` is deliberately pure so the app and a local session compute IDENTICAL answers.
- **The two API paths that stay out of the app are research and synthesis** — `researchAgent.ts` (at `deep`: 40 searches, 25 fetches at 20k tokens each across up to 12 `pause_turn` rounds, searches billed at $10/1k) and `researchLanes.ts` synthesis (every source's full text, plus a retry on audit failure). Those are the only two paths that can spend dollars per click, and research is the only one that runs unattended on a schedule. Set `RESEARCH_SCHEDULES_DISABLED=true`.
- **The app has TWO public API consumers, both marketing** (2026-07-29): the intake engine (`services/practiceIntake.ts`) — the Acquisition Engine — and **Ask about this report** (`services/reportQA.ts`, `POST /api/practice/reports/:slug/ask`), the FAB on a published research page. Both are lead-facing, so a spend cap there silently breaks the funnel rather than Paul's work; **they want their own key** — the 2026-07-27 outage took down chat, extraction AND the public funnel together because everything shares one `ANTHROPIC_API_KEY`. Both fail SOFT — no key means the intake falls back to its scripted close and the report agent returns a 503 the panel states plainly, rather than either surface implying the report is silent. reportQA sends the WHOLE report as context (~26k tokens, prompt-cached) instead of retrieving chunks: these documents carry correction notices retiring their own earlier figures, and a retriever that surfaced a retired figure without its correction would actively mislead.
- **STUDIO IS RETIRED FROM THE CHROME, NOT DELETED (2026-07-31).** `client/src/components/v6/appSurfaces.ts` exports `STUDIO_IN_APP = false`; the desktop header tab and the mobile More → Modules row read it. Every Studio screen, service, route and migration stays where it is and still compiles — `/api/research` and `/api/studio` remain mounted, so direct URLs and existing exports still work. Flip the constant to `true` and Studio returns intact. The Studio section further down is **a record of what was built, not a roadmap.**
- **Dormant, not deleted** (Paul: "let's don't delete anything yet"): `server/services/corpDevDocs.ts` duplicates the PLAYBOOK specs and lacks the target map — `PLAYBOOK.md` is canonical.
- **Known gap:** LinkedIn analytics (`server/services/linkedinAnalytics.ts`, the dependency-free XLSX reader) has **no local equivalent** — it is the one Studio capability that exists only in the app. Until a local CLI exists, an analytics import is the one reason to open the app's Studio.

## Three Workflows (operating model — Paul 2026-07-22; item 1 rewritten 2026-07-31 by THE SPLIT, see above)
The app is **Paul's private instrument — it will never be sold or distributed** (this supersedes any residual "product" reading; practice mode is now the permanent posture, not a toggle).

**Restated emphatically by Paul, 2026-07-31, with three operative consequences:** *"The app will never be sold to a third party or sold for other people to use. I will use it — if I ever am successful someday, I'll bring in a partner, and they'll use it. But it's just for me to run corp dev through. I need it to be a CRM tool and then a deal management tool. I will need to communicate with lawyers, attorneys, etcetera, and third-party folks, but it will not be outsourced and used by other firms."*

1. **NEVER MULTI-TENANT.** No orgs, no workspaces, no tenant column, no per-customer isolation, no seat model, no reopening the Stripe ladder or the `/mcp` agent surface. Enforced in code, not just doctrine: `practiceModeEnabled` defaults ON (`PRACTICE_MODE !== 'false'`), `practicePerimeter` 403s any non-team JWT, `retiredSurface` 410s `/api/chat/anonymous` and `/api/stripe`, and `mothballedAgentSurface` 410s `/mcp` and the discovery endpoints. If a request ever arrives that reads as "make this work for another firm," it is out of scope by law.
2. **A PARTNER IS THE ONLY GROWTH PATH.** Not customers — one or two people inside the practice. `TEAM_ALLOWLIST` is already that mechanism, so the app must stay multi-USER (owner columns, `user_id` scoping, activity attribution) while never becoming multi-TENANT. Those are different things and only the second is forbidden.
3. **THIRD PARTIES ARE CORRESPONDED WITH, NEVER ONBOARDED.** Lawyers, CPAs, appraisers, lenders, sellers' advisors — the practice talks to them constantly, and none of them gets an account. The existing pattern is the right one and should be extended rather than replaced: **email out + token share links** (`emailService.sendEmail`, `documentShareService`, `document_shares`, `transaction_tokens`), which is exactly why the practice perimeter lets tokenless requests fall through to each route's own auth. `service_providers` + `service_referrals` (migration 021) is the typed register for these people — attorney/CPA/appraiser with `practice_areas`, `industries`, `deal_size_min/max` and a `sent → viewed → engaged → completed` ladder. **Do NOT build counterparty comms on `direct_threads`/`direct_messages` (migration 085)** — that is a product-era social DM feature that presumes both sides have accounts, which is precisely the shape this rule forbids.
   THE LINE still governs the content: Yulia drafts, records and sends on the practitioner's instruction; she never initiates or negotiates with a counterparty on her own, and never quotes or collects a fee.

The business splits into three separate workflows with separate infra and separate cost profiles — do not blur them:
1. **The app** — marketing, **plus the CRM and the deal pipeline** (2026-07-31). Not the document surface: Studio's work moved to disk and Studio is out of the chrome. What the app owns is state that changes and gets queried — client pipeline, deals, model math, exports.
2. **LinkedIn marketing collateral** — the studio pipeline. Lives OUTSIDE the app because the app's research / deck-design / image-gen API calls are the expensive part. Rendering is pure local Chromium (no model, no app key); models only enter for research or generated art, run in a Cowork session on Paul's own subscription. Source of truth = specs in `scripts/studio/` (versioned); persistence = Paul's Google Drive **"smbX Studio"** folder — **Media = assets in, Collateral = finished posts out** (folder ids in `scripts/studio/README` context). Repeatable-and-robust doctrine: formats are brand-locked templates (`build-deck.mts` = carousels, `build-onepager.mts` = single-image posts), each post is a tiny spec (copy + image + modes), re-runnable in ~30s. **Drive constraint (real, learned 2026-07-22):** text (specs, captions) pushes to Drive cheaply through the agent; multi-MB PNGs/PDFs do NOT (base64-through-context is prohibitive) — finished binaries reach Paul as chat downloads he files into Collateral, or are regenerated from the spec on demand.
3. **The external website** — the practice site (`client/src/practice/`), Paul's customer-conversion tool like any marketing site. Separate infra, separate job.

## Core Architecture
- `client/src/components/v6/V6App.tsx` is the current app shell — a thin viewport fork into `v6/desktop/AtlasApp.tsx` (≥1024px) and `v6/atlasmobile/AtlasMobileApp.tsx`. All catch-all routes go through it; logged-out users see `client/src/marketing/` instead (two-surface rule in `App.tsx`).
- Node.js + Express (ESM) backend
- React 19 + Vite 7 + Tailwind CSS v3 + Radix UI frontend
- wouter for client routing
- PostgreSQL via raw postgres-js (no ORM)
- Claude API (primary), Google Gemini (secondary), OpenAI (tertiary)
- Billing DORMANT: the Stripe subscription machinery ($99/$249/$749/$3,000 ladder) is retained but unmounted in practice mode — nothing in the app charges money (rule 1)
- JWT authentication (no sessions, no passport — sessions broke on Railway)
- Railway deployment (GitHub push → auto-deploy, Dockerfile with Chromium)
- Auto-migrations on server startup (server/index.ts runs all SQL in server/migrations/)
- Tabbed canvas system — tools open as persistent tabs in the right canvas panel
- 11 interactive canvas model components backed by legacy pure calculation helpers and zustand state
- 5-stage sourcing engine with Google Places integration
- Premium PDF export via Puppeteer (headless Chromium) + Chart.js

## Critical Rules — Read These First
0. **THE SPLIT (2026-07-31).** **Documents are files; pipelines are rows.** ALL Studio work — masters, corp-dev documents, theses, collateral — happens in Cowork against `~/Documents/smbx-studio`, whose own `CLAUDE.md` + `PLAYBOOK.md` + `FORMATS.md` + `DESIGN.md` carry the citation law, THE LINE and the specs, because no server prompt enforces them any more. **CRM and deal management happen in the app, and building there is expected** — a CRM calls no model. Research and master synthesis stay local because they are the only paths that can spend real money. Read `WHERE_THE_WORK_HAPPENS.md` before deciding where new work goes; it supersedes the earlier "marketing only" rule.
1. **COMPENSATION (THE LINE v2, 2026-07-11).** smbX is a practice, not a product — **nothing in the app charges money.** Practice compensation is per-engagement and human-papered: buy-side retainer + buy-side success fee paid by the practice's own acquirer client (engagement letter; one-time §15(b)(13)/state-registration counsel confirmation pending — see `THE_LINE_POLICY.md`). Forbidden absolutely: sell-side, two-sided, or neutral-intermediary compensation; % of capital raised; referral fees; Yulia quoting or collecting any fee. All product billing (subscription ladder, Stripe checkout, the one-day July-10 flat-fee marketing model) is retired/dormant — `SMBX_PRICING_LOCKED.md` is a historical record with a retirement banner. No wallet, ever.
2. **WALLET IS DEAD.** walletService, paywallService, dealExecutionFee, platformFeeService deleted. Never recreate.
3. **PRACTICE MODE (default ON).** The app serves the team allowlist only: signup/login gated and every authenticated request perimeter-checked (`server/services/practiceMode.ts`; `TEAM_ALLOWLIST` env, fallback `paulbryantbaker@gmail.com`); team runs at full entitlements — no paywall, no free-tier gate; anonymous chat and Stripe checkout return 410. `PRACTICE_MODE=false` restores the retired product posture — a deliberate act, not a default.
4. **V6App.tsx is the ONLY current app shell.** Never create parallel layouts. All UI changes go through the V6 shell/components.
5. **NEVER use position:fixed full-viewport divs with background-color.** Safari reads them for toolbar tinting and it breaks dark mode switching. Use position:absolute inside a relative parent instead.
6. **ValueLens (NOT Bizestimate).** The old name is dead.
7. **The public funnel is the practice site (`client/src/practice/`).** Logged-out visitors see the corpdevservices landing + five buyer-segment pages (`/buyers/*`), converting into two actions: the **Claude-backed intake** (publicly labeled the "smbX Acquisition Engine" (renamed from "Target Mapping Engine", Paul 2026-07-13, to match the hero and drop insider jargon) — the Yulia name is app-side only; `/api/practice/intake` + `/intake/stream` SSE, Haiku, intake-only system prompt in firm system voice — no advice, no specific-target valuations, no fee talk; delivers the 8-block MARKET MAP artifact (streamed so it assembles on screen; PDF via `/api/practice/map-pdf`); the server closes deterministically — lead persisted, ENGAGED_LANES conflict check — the moment an email appears, with a scripted fallback if the model is down) and Book a call (`VITE_BOOKING_URL`, defaulting to Paul's live Google Calendar booking page `https://calendar.app.google/rA9vC7RRdR2wLJbV6` — the old #book lead form is retired; v3 sends all booking asks to the `#cta` booking card). Copy law (Paul, 2026-07-11 + confidence pass 2026-07-12): never describe a competitor — describe the work (grievance copy is banned; every differentiator is a fact about us, never a criticism of them); the category is a corporate development function, not a cheaper investment bank (be generous about banks — "different function"); AI-explicit language stays out of customer copy except the outcome claim in the process section ("we cover a market in days… inside a week of the mandate"); the intake tool is named the "Acquisition Engine" across the fold (hero sub, card, CTAs) — a system/tool framing, not an overt AI claim (Paul dropped the "Corp Dev agent" wording 2026-07-13 as insider jargon); no needy self-reassurance lines by default ("That hasn't changed." — EXCEPT the sanctioned Track Record closer "Every transaction above was done for a buyer. That hasn't changed.", Paul 2026-07-13, where it lands as a buy-side punchline); the buy-side loyalty is stated warmly, never as a stacked oath; section labels are real headers, no eyebrow micro-text. No product marketing, and NO PUBLIC PRICING (the 2026-08-05 public-schedule amendment was REVERSED by Paul 2026-08-06 after one day live): the landing's `#pricing` section is only the email-gated brochure request — the schedule itself lives in the brochure + engagement letter (see "The published fee schedule" under Product naming), and the intake/Yulia never quote fees in chat; `/api/chat/anonymous` stays 410. Retired product pages remain unrouted in `marketing/`.
8. **Yulia never says "As an AI."** Expert M&A deal intelligence. Adapts persona by league.
9. **Financial data: zero hallucination.** Extract exactly from documents, never invent numbers.
10. **All money stored in cents (integers).** Never use floating point for financial values.
11. **Mobile browser first.** Design for mobile, then adapt to desktop.
12. **THE LINE v2 is practice law.** Read `THE_LINE_POLICY.md` (v2, 2026-07-11; v1 archived at `docs/_archive/THE_LINE_POLICY_v1.md`). The perimeter: buy-side only; one buyer per target; never sell-side, two-sided, or neutral-intermediary; targets under $250M revenue; no unlicensed opinions (securities, tax, legal, appraisal → coordinate the specialist). Yulia remains the instrument: analysis, options, implications, models, drafts — she never contacts or negotiates with counterparties on her own, custodies funds, signs, files, or sets/collects compensation. The practitioner runs the deal and owns the judgment.

### Track Record attribution doctrine (LAW, Paul 2026-07-13; anonymization REVERSED 2026-08-07)
**EMPLOYERS ARE NAMED AGAIN (Paul, 2026-08-07, on the Carta-restyle handoff: "Lets name — i have hidden any deals or other information that is basically not on my resume").** This supersedes the 2026-07-18 anonymization ("a global investment bank" / "a world-class PE-backed aggregator"), which is now HISTORY — do not reinstate it, and do not "correct" a named employer back to a euphemism.
**The reasoning is the boundary, so keep it:** what may appear publicly is what is already on Paul's résumé. He curated the handoff's copy himself and removed anything that is not. **Wrench Group · JPMorgan Chase · Deloitte Consulting** are therefore named, and the deal-name lists in `design_handoff_smbx_carta_restyle/Track Record - Carta Style.dc.html` are **the CLEARED SET**. Port them verbatim: never merge in names from the older `TrackRecord.tsx`, never add a name from a deck or a memo, because the filter that produced that list was Paul's judgement about his own résumé and no other source has passed it.
**The other five rules are untouched and still bind:** (1) always "led or co-led", never unqualified "closed"; (2) always **"selected transactions"** (the design's register cards already carry the header); (3) the attribution shield appears WHEREVER the deal names appear, never as a footnote; (4) names, not logos; (5) never "our clients" for an employment deal; (6) JPMorgan is integration (Director of Acquisition Integration), not origination.
**Stat set (Paul, 2026-08-07, "all good here"):** the public figures are **150+ acquisitions & integrations · $5B+ enterprise value added · ~$21B transactions touched · $2B synergies captured · 0 sell-side engagements, ever.** The earlier no-plus rule ("150", no "+") is superseded; `$2B synergies captured` joins the sanctioned set.
The Track Record page (`/track-record`) and the landing track-record band present Paul Baker's employment deals — they were NOT smbX engagements — so six rules are non-negotiable and enforced in `TrackRecord.tsx` (see its header + the exported `AttributionLine`/`ATTRIBUTION`): (1) always "led or co-led", never "deal captain on every one" or unqualified "closed"; (2) always "selected transactions" (the wall is a selection, which reconciles it with the 150+ figure); (3) the attribution shield sentence appears WHEREVER the deal names appear (landing band, track-record page, any deck/PDF) — never a footnote; (4) names, not logos; (5) never "our clients" for a Wrench/JPMorgan transaction; (6) JPMorgan is integration (Director of Acquisition Integration), not origination. The interactive filter/search deal sheet (old task #27) was replaced by static per-employer selected-transaction lists so names sit under the employer they belong to. The proof stat strip now ends on "0 · sell-side transactions. Ever." (was "1 · side of the table").

## Design System
**CARTA (2026-08-07, `design_handoff_smbx_carta_restyle/` — THE CURRENT PUBLIC-SITE
LANGUAGE; Aurora below is now the app/collateral language only, pending phase 2.)**
Paul's correction set the method: *"this is not what Claude Design shipped.. it
shipped a new design for the existing content"* — so the five logged-out pages
(Landing, About, Industries, Research, Track Record) were REBUILT by transcribing the
`.dc.html` references 1:1, not by evolving the Aurora components. The system:
Source Serif 4 (550/600 display) · Schibsted Grotesk (working) · IBM Plex Mono;
bone `#FCFAF6` / panel `#F3F0E9` neutrals with cooler ink scale (`#16181A/#4A4F54/#7C8187`);
ONE accent (Deal Green `#0A7A58`, hover `#086348`, tint `#DFF5EC`, mint `#A8F0CE` on
dark) — brass/honey are GONE from the site; flat near-black dark bands `#131512`
(square edges, no texture, no curves); radius 0 except buttons/inputs; corner-handle
frames (8px ink squares at −4px — the house gesture that replaced the curved band
crests); dot-field ornaments + orbit; hairline 1px grids. THE BUTTON LAW: green is
NEVER a resting button fill — primary is ink-on-light / bone-on-dark, green appears
on hover, chips, kickers, links. Tokens live in `house/tokens.ts` (`CARTA`,
`CARTA_TYPE`, `CARTA_HANDLE`); the `.pd` var layer mirrors them; layout values live
INLINE in the transcribed pages per the handoff doctrine, and `carta.css` carries only
hover/media/keyframes plus the `.ca-engine` intake redress. Build record:
`design_handoff_smbx_carta_restyle/IMPLEMENTATION_PLAN.md`. Deferred:
collateral renderers (LEDGER stays their source until phase 2 — see the interim
notice atop `content/studio/DESIGN.md`), report bodies + `/buyers/*` interiors.

**THE DESKTOP FOLD (2026-08-08, Paul: "the hero / above the fold is a little
boring on desktop… while carta is actually a little busy, we can meet in the
middle — making the ball bigger balancing out the page (golden ratio)… lean
into the Carta look and feel, blending our old with it to make an original").
Boring had a mechanical cause, same as mobile did: two near-equal columns
(1.02fr/.98fr), the type at its loudest, and the only geometry a 150px orbit
tucked behind the engine card's corner. So the pass is PROPORTION, not
content — nothing was added to read, which is precisely the half of Carta we
are not copying. Columns → **1.32fr 1fr** (φ is 1.618 and starves the card to
467px, where its lane chips break two-deep; 1.32 is the meet-in-the-middle),
H1 `clamp(35px, 4.05vw, 82px)` (69→58px at 1440), lede 20→19px at a 28em
measure, and the orbit grows into a **hero-scale ring** — `clamp(560px, 55vw,
860px)`, our gesture wearing Carta's square handles as its satellites.
**THE RING IS NOT CONCENTRIC WITH THE CARD, and that is the whole trick**
(Paul, same day: *"why is the ball hiding behind the chat box?"*): the first
cut centred it, and an even rim around an opaque panel reads as a HALO ON the
panel — the eye gets no cue as to which is in front. Offsetting it left leaves
~230px of ball on one side and ~30px on the other, and that asymmetry is the
depth. Two corollaries, both learned by rendering: a node must sit ON a
visible arc or it reads as a smudge (the third one, orphaned by the offset,
was deleted), and the lede's measure had to come in to 28em because the ring's
widest point crossed its first line.
**Every new decoration is DESKTOP-ONLY and the two marks it replaces hide at
the same breakpoint** (`.ca-orbit-hero`/`.ca-hero-hdots` vs
`.ca-orbit-corner`/`.ca-hero-cdots`, complementary media queries) so exactly
one set shows at any width and the phone hero is byte-identical. The ring
starts at **1280**, not 1025, for a geometric reason rather than a taste one:
below that the card is ~470px and opaque, so any ring that fits the fold is
barely wider than the thing hiding its middle — at 1100 a 510px ring showed a
66px crescent and two stray arcs, which reads as debris. 1025–1279 keeps the
corner mark and relaxes to 1.08fr.
**The pass also caught a live nav defect across the most common laptop
widths.** The reference's 1180/1080 compression steps were transcribed before
our nav grew a Research link, a Free Valuation link and a 38px logo, so the
links SPILLED — `[data-nav-links]` carries `min-width: 0`, the flex item
shrinks below min-content, and its `white-space: nowrap` children hang past
its right edge. **The container box never overlaps, so measuring it reads
clean; only the last CHILD's right edge against the CTA cluster shows it.**
Two bands were live: 1081–1112 and 1181–~1290 — the second catching **1280**.
Ladder moved to 1310/1150; measured clean (worst margin −38px) at every width
from 1024 to 1920.

**MOBILE IS IN (2026-08-08, Paul: "can we start on Mobile?" — closes Q3, the
deferral the plan flagged as "the biggest gap"). THE FIX WAS FLUID, NOT A
BREAKPOINT, because the cause was one mechanical thing: EVERY CLAMP ON THE SITE
HAD A DESKTOP-SCALE FLOOR.** `clamp(130px, 12vw, 200px)` never goes below 130px
and 12vw only reaches 130px at a 1083px viewport, so below ~1080px the vw term
was always under the floor and the floor always won — a 390px phone rendered the
desktop MINIMUM of everything (130px section padding, 48px headlines, 32px
gutters, four-column grids). The floors were re-cut IN THE PAGES (type ×0.72,
space ×0.52, gutters to `clamp(20px, 4vw, 32px)`); **a floor only binds below its
own crossover width, so desktop is untouched** — verified by diffing the
geometry of every laid-out element on all five pages at 1440px before and after:
identical but for the two deliberately deleted arc nodes. `carta.css` then holds
only what a clamp cannot say: the grid collapses at ≤900 (`[data-g3]`
`[data-phase]` `[data-fnd]` `[data-split]` `[data-numrow]` `[data-deflist]`,
`[data-who-grid]` at ≤640), the `min-width: 0` guard, the card interiors, and the
phone sheet's Carta redress. Three traps, all of which RENDER rather than error:
(1) a grid item defaults to `min-width: auto`, so one wide descendant stops the
track shrinking and the column pushes past the section padding — the hero's
engine card measured 370px in a 350px box, flush right with a 20px gutter left;
(2) **an IDENTITY `transform` still makes an element the containing block for
`position: fixed` descendants** — the `data-hs` hero cascade landed on
`translateY(0)` and left it there, which pinned the intake's phone bottom sheet
to the 350px card column instead of the viewport (shipped broken with the Carta
transcription; the cascade now clears the transform on `transitionend`); (3) the
`.ca-engine` redress is scoped `min-width: 901px`, so the phone sheet — the most
important surface on a phone — was the one thing still wearing Aurora (it now
has a phone sibling block; its 22px top radius is the ONE sanctioned exception
to radius-0, Paul 2026-08-08: "yes the top corners need to be rounded on the
phone sheet" — the curve is what says the panel slid up and can slide back
down). Nav ≤760 drops both CTAs to the burger (they already live in the menu).
**THE GATE IS `npm run shoot:mobile`** (`scripts/mobile-audit.mjs`) — and after
Paul's *"I can't take a picture of everything that looks awful… let's do an
antagonistic pass"* it is no longer an overflow reporter but a hunter for eight
NAMED failure classes across ALL FIFTEEN public routes (landing · 4 inner pages
· 3 report bodies · 5 `/buyers/*` · 2 legal): **BLEED · GRID · RAGGED · SQUEEZE
· COLLIDE · CLIP · VOID · TAP · TINY**, exit 1 on any finding. 719 → 246, with
every structural class at ZERO across 360/390/430px. The three defects a
screenshot could never have shown, all of which render rather than error:
a `white-space: nowrap` link winning a flex row and leaving the paragraph a
104px two-words-a-line column; the pricing band's email field crushed to 145px
by a `flex: none` button (an input has no text, so no text check could see it);
and `#who`'s ornament chips anchored at `left: 1%` of a **1360px rail** — 14px
from a desktop headline, 3px from a phone one, as solid green blocks under the
letterforms. **Four checks had to be taught what is DELIBERATE** and each
correction is commented in the source: content scrolling inside `.rp-tablewrap`
or the marquee's `overflow:hidden` is not a bleed (it flagged all 31 of a
report's registers); a closed `<details>` still answers `getBoundingClientRect`
with a laid-out box in Chromium; inline `<em>`/`<strong>` wrapping two lines
legitimately overlap; a grid of NUMERALS is fine at 158px where a grid of
sentences is not. The `.rp-*` report document scope was audited and needs no
structural work.

**PRACTICE SITE (2026-07-11, corpdevservices bundle — the prior public
surface, now HISTORY beneath the Carta restyle.)** The THE LINE v2 pivot retired the product marketing; the same day
Paul shipped the practice-site design (`corpdevservices/`, approved direction
3a) and it is implemented at `client/src/practice/`. DEFINITIVE is untouched.
The app shells (Atlas) continue as the working surfaces for the practice.

**AURORA (2026-07-31, Paul: "I hate the dark green… it's too dark and slight
green tint is too dark… what can we do to make the pages look more cheerful and
AI forward DEAL making goodness?"). THIS IS THE CURRENT PALETTE — the LEDGER
TRIAL entry below is now a historical record of what it replaced.** The
complaint was about WEIGHT, not hue, so the Ledger structure survives intact
(bone canvas, white hairline cards, one accent, warm jewelry, full-bleed rhythm
bands, Fraunces/Inter/Plex) and only the values move: **nothing in the system is
near-black any more.** The green-black boardroom band became a saturated **jade
block `#0A6A4C`**, the accent lifted to **Deal Green `#0A7A58`**, brass grew from
jewelry-on-one-stat into a working **amber `#E8A62B`** (`#F5C452` on the block),
the canvas brightened to `#FCFAF6`, and a jade/amber ambient bloom replaced the
tonal-bone wash. Two new tokens: **`jade #0FA97C`** — large numerals, edges and
ambient only, NEVER small text (white on it is 2.97:1, it as link text on bone
is 2.85:1, and neither failure is visible in a diff) — and **`honey #F5C452`**,
amber's on-block value. Retired into the dead list: `#16624C` `#0F4E3C`
`#0F1A16`.

**TWO TRAPS, both of which render rather than error.** (1) The dark texture is a
near-black image that sits ABOVE the colour in the background stack, so
`background: DARK url(texture)` ignores the colour entirely — swap the token to
jade and every dark surface stays exactly as black as before, with nothing in
the diff to catch it. The block is therefore a COMPOSITE: an 0.84 glaze of the
base over the texture, defined once as `blockBackground()` in `house/tokens.ts`.
Never hand-roll the stack. (2) The alpha ceiling on the ambient wash is set by
the 13.5px caption tier (`--pd-tert`), not the body tier — body text still reads
at 5.2:1 where captions have already dropped under 3:1, so checking the obvious
one passes a wash that fails.

Sources of truth unchanged and all four move together or the gate fails:
`house/tokens.ts` (the values), `client/src/practice/practice.css` +
`report.css` (the site), `content/studio/DESIGN.md` (the law that travels to the
workspace), `DESIGN_LANGUAGE.md` (the root brief). `npm run test:design` is the
gate — 58 cases asserting every hex is a real token, every token is documented,
no retired hex appears as live guidance, and the live stylesheet still matches.

**SALES-JOURNEY REFLOW (2026-08-07, Paul: "between the 2 green bands there is a
whole lot of space and the sections kind of run together… the getting started
should be part of How it Works… add a green to break up monotony where it makes
sense… the pricing section is just stuck on the page somewhere"; vetted by a
3-lens critique workflow).** The landing is now built for the reader who never
chats — each section answers the next question a cold visitor has: hero → jade
`#proof` (trust) → `#why` (the case) → `#how` (what you get — the seven phases
now CONTAIN the Getting-started sample read as a sub-block, `#sample` kept as a
live anchor, and the section closes exactly ONCE: phases → sample → verdict
paragraphs → CTA cluster) → **`#pricing` as the THIRD jade band** (the
email-gated brochure ask in a full-bleed room of its own, directly after the
work it prices; no figure on the site, unchanged) → `#who` → `#sectors` →
whose-side (jade) → `#owners` → founder → `#cta`. Band cadence jade → 2 light →
jade → 2 light → jade → 3 light, inside the "2–3 points in the scroll" law.
Mechanics: `.pd-dark-sect` is the ONE band-approach margin
(clamp(90px,10vw,150px); both mid-page bands wear it — it replaces the
whose-side band's inline marginTop, the only inline band margin that existed,
and the pricing section's old `.pd-section` padding-top — the curve crest
supplies the rest of the transition); `.pd-getting` holds the sub-block margin
plus the nested `#sample` scroll-margin; `.pd-dark .pd-price-get` carries FOUR
declarations, every one a renders-not-errors trap that read clean in a diff:
the "Sent" state and the input border (`.pd-dark` does NOT re-scope
`--pd-coral`, so the confirmation rendered Deal Green on jade at ~1.1:1), the
typed email + caret hard-coded to ink (the input's `color: var(--pd-ink)`
re-scopes to near-white on the white fill — idle screenshots pass because the
placeholder is UA grey), and a re-stated `:focus-visible` border (the
transparent-border override sits LATER at equal specificity, and the light
rule also kills the outline, so keyboard focus showed nothing on the band). The "Get pricing →" link at `#how`'s close was
removed as redundant — the band is the next screenful; the anchor stays live
for nav/footer/emailed links.

The three current languages (the LEDGER TRIAL entry records the system Aurora
re-valued, not a system to build against):
- **LEDGER TRIAL (2026-07-17 night, Paul: "let's do what the recommendation says and see how we like it" — the fintech-DL research system, implemented faithfully for evaluation):** bone canvas `#F6F4EF` (tonal-bone ambient, no chromatic wash), ink `#14181C`/slate `#5C6670`/muted `#8A9099`, WHITE hairline cards (radius 12–16, Ledger soft-lift shadow), ONE accent **Deal Green `#16624C`** (hover `#0F4E3C`, tint chips `#E7F0EC`, mint-on-dark links `#8FD0AE`) in the historical `--pd-coral*` slots, **brass `#B08637`** jewelry on the signature "0 sell-side" stat (`--pd-brass`), green-black boardroom dark bands (`--pd-dark-* #0F1A16` family, ivory text `#F3F1EA` rescope, green halo + brass base glow + green-black glaze over the blackbleed texture), working type switched Schibsted→**Inter** with global `tnum` tabular figures, Fraunces display at weight 545, **pill buttons** (Paul 2026-07-18 overrode Ledger's 8px: "no squared buttons"; ghost secondary keeps the ink hairline), 16px white chat console (was 999px pill), solid bone nav (backdrop-blur removed per Ledger anti-patterns), logo X recolored to the green (`/logo-green-x.png`, `/logo-x-green.png`; coral+blue masters remain). Composers (researchComposer, practiceMapPdf) follow. Deliberately skipped from the doc: Sell/Raise chips + client-logo walls + pricing (THE LINE / attribution law), the dark hero (doc's own light-mode conversion caveat; light hero kept), Inter-for-Schibsted in the APP shells (site only). Rollback = revert one commit per system (blue and coral live in git history).
- **OFFICE-BLUE PIVOT (2026-07-17 evening, Paul: "all of the pink, the red accents… no more terra cotta anything. think Microsoft Office"):** the coral accent system is RETIRED sitewide. Accent = professional Office blue `#185ABD` (hover `#124A9E`, links/labels `#1656B4`, bright-on-dark `#9EC1FF`) held in the HISTORICAL `--pd-coral*` var names (values swapped, names kept to avoid churn — rename to `--pd-accent*` in a quiet moment). Atmosphere = CHAMPAGNE-GOLD only (hue ~42°; no pink, no clay/terra-cotta hues anywhere); dark bands carry a steel-blue top halo + champagne base. Chips blue-on-ice (`#1656B4` on `#EDF3FC`). Logo: the X recolored to the blue — `/logo-blue-x.png` + `/logo-x-blue.png` (generated pixel-exact from the coral masters, which remain on disk as history; footer white-inversion law unchanged). Server composers (researchComposer, practiceMapPdf) carry the same blue + blue logo. The "coral" language name below is historical.
- **WARMTH PASS (2026-07-17, human reviewers: "cold, boring… warm it up"; hue law same day, Paul: "the hard pink is a no"):** the coral system's neutrals all moved onto the warm axis — paper base `#FDF8F3`, warm ink/body/tert/card/border tokens (R≥G≥B), ambient wash raised to ≤0.085 alpha and **hue-locked to APRICOT/HONEY/PEACH only — no coral/rose/pink in ANY ambient layer** (hero bloom, heromesh dots, section accents, dark-band halos/sparks all amber; coral acts only where the brand acts: CTAs, links, labels; AA re-checked against the new body `#5D5651`), dark bands warmed from neutral charcoal to ember near-black (`--pd-dark-*` R>G>B) with an ember halo + base glow + low warm glaze over the blackbleed texture, chips rust-on-cream (`#A2542A` on `#FFF2E6`), and the display ladder (`.pd-h1/.pd-h2/.pd-quote/.pd-statement/.pd-cta-h`) moved to **Fraunces serif** (`--pd-display`, weight ~600, near-normal tracking, more leading) while ALL working type (h3, body, UI, numerals, mono) stays Schibsted; chips warmed to peach tint; theme-color meta + boot bg fixed from stale V6 slate `#F6F7F9` to the warm paper; a founder photo band (`.pd-fndband`, real Times Square walking shot at `/founder-walking.webp`, sanctioned deal-captain copy) sits before `#cta`. Sizes/ladder law unchanged.
- **Practice site = "coral" (2026-07-11, corpdevservices + Paul's copy rework):** `client/src/practice/` under the `.pd` scope — white canvas, ink `#222222`, card gray `#F7F7F7`, accent coral `#FF385C` (links/hover `#E61E4D`, CTAs solid `#E61E4D` → hover `#D70466`; the gradient was retired from CTAs/surfaces by the 2026-07-12 intensity pass; the 2026-07-13 atmosphere pass sanctions AMBIENT-only coral blooms accenting section text plus procedural SVG band art — footprint constellation, mesh — in `atmo.tsx`/`.pd-accent`), Schibsted Grotesk 400–800 display/body, IBM Plex Mono for tags/badges only (section labels are header-weight `.pd-seclabel`, NOT mono eyebrows — Paul's law), pills 99px / cards 24px / bands 28px, airy spacing. Conversion mechanism: the Claude-backed Yulia intake card (`/api/practice/intake`) + Book a call; leads persist via `/api/practice/leads`. Sources of truth: `corpdevservices/README.md` for layout, Paul's 2026-07-11 reworked copy deck for words (AI demoted to one mention; loyalty warm, not sworn). Intensity pass (2026-07-12): proof numerals at 92–168px, three near-black full-bleed sections (track record, whose-side, final CTA via `.pd-dark` var re-scope), problem ledger as an analysis table, horizontal process sequence, dense industries list, editorial pull-quotes, explorable deal sheet on /track-record, and the streamed Market Map artifact. Photography: documentary shots of real businesses wanted (real or none — no stock; `ImageSlot` was deleted); `VITE_BOOKING_URL` awaiting the scheduling link. **Slack homepage reflow (2026-07-13, Paul: "mirror Slack as much as possible"):** the landing (`Landing.tsx`) now reads on Slack's centered rhythm — a centered hero (`.pd-hero-c`: statement H1, two CTAs `.pd-cta-row`, an HONEST proof row `.pd-proofrow` since a fabricated logo strip would break zero-hallucination) over a tabbed product showcase (`.pd-showcase`/`HeroShowcase`: live engine ⇄ sample read), a curved `.pd-swoosh` into each dark band, centered section intros everywhere (`.pd-sechead`/`SectionHead`), and Slack's signature left-vertical-nav feature showcase (`.pd-feature`/`EngagementShow` for the engagement, `.pd-whosel`/`WhoSelector` for the segments — a hovered/focused row swaps a framed stage panel; the stage is an honest phase description, never a fake screenshot). The three `.pd-dark` bands and the two protected `.pd-askew` dramatic compositions (180 value band, whose-side) stay as the punctuation between centered sections. The old asymmetric `.pd-ledger-head` mid-page and the redundant standalone "first read" section are retired (the showcase's sample tab covers it); `.pd-index` was repurposed as the numbered scenario stepper. **Per-type How-it-works + segment detail (2026-07-13, Paul: "these need more detail… an overview of the scenarios for the various customer types… we sell corp dev as a service and need to be faster and more nimble"):** each buyer segment now carries a `scenario` (the engagement mapped to that buyer's situation, five tailored steps, threaded with the speed edge), `gets` (deliverables), a richer per-segment `The work` answer, and a tailored `ctaH`/`ctaBody` close (Paul's clean-master copy, 2026-07-13) in `segmentData.ts`; the landing Industries hunt board carries Paul's expanded per-trade theses. The landing's generic three-step "process" is replaced by `HowItWorks` — centered pill-tabs (`.pd-showtabs`) swap the tailored scenario per customer type — and each `SegmentPage` gained a full "How a deal runs for you" walkthrough + a "What you walk away with" deliverables grid (`.pd-gets`). All scenario copy stays inside THE LINE (the client decides and signs; we coordinate the licensed specialists; buy-side only; one client per target; no securities/tax/legal opinions or specific-target valuations). **Slack-grade atmosphere pass (2026-07-13, Paul: "the gradient, the bleed sections, the dividers — everything big and clearly legible"; spec vetted by a 4-lens design workflow):** (1) a full-page ambient coral wash — ONE `.pd-ambient` layer, `position:absolute; z-index:-1` inside the now-`relative; isolation:isolate` `.pd` (warm-white base `#FFFDFC`), six distributed no-repeat coral→rose→peach radials, every stop ≤0.050 alpha so body text holds AA (~5.0:1); Safari-safe (never a fixed colored div) and deliberately UN-PROMOTED (no will-change/transform/filter — that would allocate a ~57MB backing store on the tall scroll); the local `.pd-hero`/`.pd-accent` blooms were de-stacked so coral never compounds. (2) Curved bleed dividers bracket EVERY dark band on every page — `<Swoosh dir="in"/>` before, `<Swoosh dir="up"/>` after (exported from `atmo.tsx`); the arc fill reads the shared `--pd-dark-top/-bot` tokens so it's a seamless continuation of the band. (3) Dark bands gained depth without leaving near-black: a top coral halo + a whisper-warm vertical gradient (`--pd-dark-top #151312`→`--pd-dark-bg #141414`→`--pd-dark-bot #131313`, NOT aubergine) + an opt-in corner `<span className="pd-spark">` (the Slack "stars", a whisper). The type ladder was bumped up (see the type-scale note). **Dark-band texture pass (2026-07-14, Paul: "make the 4k artwork cover the divider and more visible; the corner constellation is not a nice texture"):** every `.pd-dark` band now carries a full-bleed dark texture — **`/textures/blackbleed.webp`, Paul's `blackBleedBG.png` slate/plaster surface** (he uploaded the 8.4MB master to repo root; served copy is an optimized 2560px webp). A subtle top coral halo keeps a whisper of brand; the base gradient is the fallback. The curved divider edges are cut from the `.pd-dark` band ITSELF via a three-layer `mask` (a fixed-height curve mask at top + at bottom, a solid fill between — union; the curves keep proportion at any band height because only the middle stretches), so the texture is one continuous crop with NO seam and the transparent corners reveal the ambient wash. The old standalone `.pd-swoosh` element is retired — `Swoosh()` is a no-op kept so existing placements stay valid. `.pd-dark-pad` = `var(--pd-curve) + clamp(40px,5vw,84px)` so content always clears the curve. The procedural `FootprintMap`/`Mesh`/`BandArt` (the corner constellation Paul flagged) are retired from the bands — still exported from `atmo.tsx`, unused. **All dark-band reading text is bright white (Paul, 2026-07-14): `.pd-dark` re-scopes `--pd-ink` #FFFFFF, `--pd-body` #F4F4F4, and `--pd-coral-link` #FFFFFF (the eyebrow labels/tags that were coral read hardest on the texture) — hierarchy comes from size/weight, not colour; only `.pd-link` keeps a bright coral (#FFB3BF) for affordance.** The earlier recolored-teal `band-coral.webp` was superseded. **Chat-first hero (2026-07-14, Paul: "the home page is the CHAT like Grok or Claude… somewhere between Grok and Slack"):** the landing hero RESTING state is Gemini's move (final form, Paul 2026-07-14 evening: "results are not what I wanted" → no card chrome): statement H1 + one-line sub, then ONE big pill input bar (`.pd-herobar`, self-typing ghost, no card, no opening paragraph — the headline already says it) with centered `.pd-chip` starters beneath, and a single wrapping meta row (`.pd-hero-below`: book-a-call pill · proof · `.pd-samplelink` toggling the sample read — the pill-tab row is gone; `.pd-showtabs` lives on only in HowItWorks). On desktop the bar swaps for the conversation card the moment the visitor engages (`.pd-chat-zone.resting` CSS fork). On phones (≤900px) the bar is the doorway (readOnly input so no keyboard in-page — the ghost-click trap: opening on pointerdown let the tap's synthesized click land on the scrim and instantly re-close): any tap slides the real chat up as an always-`position:fixed` bottom sheet (`.pd-chat.open`, 92dvh, grabber + minimize chevron, scrim, transform-transitioned both ways so there's no reflow jump), every `a[href="#yulia"]` opens the sheet directly, and the body is scroll-locked iOS-style (position:fixed at the scroll offset — `overflow:hidden` alone doesn't stop iOS touch scroll). Desktop keeps the chat inline. The global `.pd :focus-visible` ring is suppressed on `.pd-chat-input` only (text inputs match :focus-visible on ANY focus, which drew a second box inside the input bar — the row's `:focus-within` border carries focus instead). **Drawer hardening + fold balance (2026-07-14 evening, Paul: "minimize and it started over… background scrolls through… fit the mobile screen better… desktop crowded/unbalanced"):** the conversation persists in sessionStorage (`smbx_intake_v1` — messages/done/draft hydrate on mount, so the sample-tab unmount, minimize, or a reload never wipes a session; the launcher flips to "Tap to continue your session"); the sheet owns its touches (drag the header/grabber down >110px to dismiss with direct-manipulation follow + spring-back; `.pd-msgs` scrolls but is edge-trapped so top-overscroll can't chain into iOS pull-to-refresh; everything else preventDefaults; html gets overscroll-behavior-y:none while open) because iOS ignores overflow CSS for touch panning; `--pd-kb` (set from visualViewport) lifts the sheet's bottom above the keyboard; the "Map your market" showcase tab dispatches `smbx:open-intake` (drawer opens directly on phones). Fold balance (final calm pass, Paul ×2 "too crowded… unbalanced"): the desktop lead-in dropped to a Gemini-scale statement (`.pd-hero-c .pd-h1` clamp 30–46px, 24em, two lines) with generous air (hero pad clamp 44–92 top / 40–76 bottom, showcase gap 30–56), so the fold reads nav → breath → statement → breath → card → clear space, with the tabs + the ONE wrapping meta row (book-a-call + proof) at the fold edge; the resting card HUGS content (the `.pd-show-stage .pd-msgs` min-height dead-hole is gone); phones get a smaller lead-in (H1 clamp 28–36, `.pd-sub` 18px) and a compact launcher showing two starter chips. **v3 — the Claude Design handoff (2026-07-16, `practiceSite/` bundle; Paul: "i like how the claude design site is set up so do it that way"):** the live site now implements the CD-built two-page design; `practiceSite/IMPLEMENTATION_PLAN.md` is the build record. Landing IA: hero (`.pd-hero-fold` sizes the fold so the proof band's curve crests at the bottom; `.pd-heromesh` dot-matrix bloom behind the input; H1 back up to clamp(42,5.2vw,68); static placeholder — the self-typing ghost is retired; chips are the five hunting lanes and SEND on tap) → dark proof band `#proof` with count-up (~$21B "transactions touched" joined the sanctioned stats) → Why-us 6-card evidence grid (`.pd-whygrid`/`.pd-why` details) → 7-phase accordion (`.pd-phases`/`.pd-phase`) → static new-format sample read (`.map-flow`/`.map-nine`/`.map-screens`/`.map-number`/`.map-verdict`) → who-index (click-swap panel; all interest routes to `#cta`) → 12-row hunt board → whose-side band → booking-card CTA `#cta` (Google Calendar link, no form) → **flat warm-charcoal footer `#2B2A27`** (var-remapped white text, inverted logo — deliberately NOT a textured band; quiet Sign in kept in the legal row as a sanctioned deviation). NEW **`/industries`** page (`Industries.tsx`, `footerCompact` shell variant): 12 `.pd-sector` zig-zag theses + the Heritage block carrying the attribution shield line; copy law — no directional market numbers there, only named regulations. Two-width law: `--pd-max: 1360` rails + `--pd-mid: 1040` artifacts, no other section widths; `.pd-h2` = 24em + `text-wrap: pretty`; section pads clamp(130,15vw,220). Nav: Why us · How it works · Industries (route) · Who it's for; consultation pill → `#cta`. Segment pages, /track-record, and /about stay ROUTED but unlinked from the chrome (direct URLs live; Track record in the footer anchors to `#proof`). Retired from the landing: problem ledger, `EngagementShow`, per-type `HowItWorks` pill-tabs, 180 value band, landing track-record band, hero proof row, sample tab (the engine⇄sample toggle died — `#sample` is its own section). The intake engine keeps ALL real mechanics (SSE map, PDF, leads, sheet hardening, sessionStorage).
- **Retired marketing = "liquid glass" (2026-07-10 hi-fi, unrouted):** `client/src/marketing/` under the `.mk` scope — the fully-built product marketing (canvas gradient, glass/ink cards, neon `#00D632`, Sora headlines, page-morphs-into-Yulia). Unrouted by the pivot; kept as a parts bin (`smbX.ai Marketing Redesign/` bundle + 2026-07-10 copy deck are its sources). Its copy sells the retired product — never reroute it as-is.
- **App = "Atlas" (2026-06-24 cutover):** desktop shell `client/src/components/v6/desktop/` themed by `atlasTokens.ts` (`T` object, Google-blue `#0b57d0`); mobile shell `v6/atlasmobile/` themed by `mobileTokens.ts` (violet `#5b53d6`, Cash-App-flavored). Build law: `ATLAS_BUILD_CONTRACT.md` + `MOBILE_REDESIGN.md`. Re-skin toward the new language is a later phase.
- **Legacy marketing = "Ramp" (2026-05-29):** quarantined at `client/src/marketing/legacy/` under `.mkt` — serves only the footer-linked pages (Raise, Integrate, Connectors, Standard×2) until restyled or retired.

**Historical (do not build against):** V6 slate-blue/lavender/periwinkle (`design_handoff_smbx_desktop_material/`, `design_handoff_smbx_app store/`, `DESIGN_SOURCE.md`, `DESIGN_TOKENS.md`), hot pink `#D44A78` (V3/V4), the Cowork-DL warm-cream "Edition" era, and the short-lived terra `#D4714E` wireframe pass (2026-07-08, superseded by the hi-fi liquid-glass green system two days later). If your output anchors on any of these, you read a stale doc.

**Safari toolbar rule still applies:** never use `position:fixed` full-viewport divs with a background color (Safari reads them for toolbar tinting and it breaks dark-mode switching). Use `position:absolute` inside a relative parent instead.

**Unified type scale (LOCKED 2026-07-13 consistency pass; bumped UP 2026-07-13 Slack-grade pass — one-size-per-role preserved).** The practice site (`.pd`) routes ALL text through one ladder — do not reintroduce per-section inline `fontSize`/`maxWidth` overrides. Roles (post-bump for "big and clearly legible"): `.pd-h1` hero clamp(46,6vw,92)·lh0.95 / `.pd-h2` section header clamp(38,4.8vw,62) (`.pd-cta-h` shares it) / `.pd-h3` minor header 22·700 / `.pd-sub` lede 22 / `.pd-body` 18·1.7 (the one body size, #6A6A6A holds AA over the ambient wash) / `.pd-body-sm` 15 / `.pd-caption` 13.5 / `.pd-mono` 13.5·0.09em. Protected drama keeps its own scale: `.pd-quote` pull-quotes clamp(44,5.8vw,78), `.pd-statement` in-flow rhetorical lines clamp(28,3vw,46) (below H2 by design), `.pd-num`/`.pd-bignum` numerals, and the self-contained Market Map document (`.pd-map`/`.map-*`, PDF-parity — never fold into site roles). Headers use ONE measure (18em); body measures may follow layout. New sections adopt these classes, never bespoke sizes. The lock is about consistency (one size per role), NOT immutability — the whole ladder may scale together, but never per-section.

**No gratuitous eyebrows or micro text (LOCKED 2026-06-01; hard floor added 2026-07-13).** Paul's readability floor: no customer-facing text below 13px — the practice site's mono label voice runs 13–13.5px with ≤0.12em tracking, and label grays stop at `--pd-body`, never fainter. Do NOT add decorative eyebrow kickers (small uppercase mono labels like `PIPELINE`, `RECENTS`, `MARKET INTELLIGENCE LIVE`) or micro status/subtitle lines (like `deal intelligence · online`) by default — they clutter and read as AI filler. Lead with the title alone. Add a label or secondary line ONLY when it carries information the user genuinely needs and cannot infer from context. When in doubt, leave it out. Applies to new components, headers, cards, chat surfaces, and FABs; when reskinning or refactoring existing UI, prefer removing these over preserving them.

## Product naming (Paul, 2026-08-05; tier + process renamed same day)
The practice's primary offering is **smbXCorpDev** — the buy-side
corp-dev-as-a-service engagement through close. With post-merger
integration support (the PMI0–PMI3 stages) it is **smbXCorpDev
Premium** (renamed from "Plus"). The proprietary process behind both is
branded **smbXDefinitive** — the single external name for what internal
docs historically call THE LINE (the compliance perimeter) and
DEFINITIVE (the methodology/substrate). BRANDING ONLY: internal doc
titles, code identifiers, file names and the laws themselves keep their
names — smbXDefinitive governs what decks, letters and site copy say,
not what the repo calls things. These are ENGAGEMENT names — not app
products (rule: the app is never sold). Owner-side vocabulary is
unchanged: the market read is the buyer artifact, the valuation is the
owner product.

### The published fee schedule (Paul, 2026-08-05 — "a set price that any
### would-be buyer would not say no to"; supersedes "no pricing page")
ONE schedule, every client, NO negotiated pricing — the no-negotiation
posture is itself a loyalty statement (the fee cannot grow by talking a
client into a bigger deal). Chosen over Double Lehman deliberately (9.1%
on a $1.8M deal reads investment-bank and prices out the searcher lanes).
COPY VOICE (Paul, same day: "dont be so combative lol"): the section
sells CLARITY warmly — "Simple, up-front pricing your whole team can
embrace", "nothing to haggle over, no surprises at close" — never the
defiant register ("No negotiation." / "the schedule is the schedule"
stay out of customer copy; the mechanics don't soften, the voice does).
- **Retainer: QUARTERLY (Paul, 2026-08-06 — supersedes the same-week
  monthly billing): $15,000 per quarter, paid up front** ($5K/month
  equivalent). The first quarter IS the engagement; each renewal is
  another $15K quarter up front; step away at any quarter's end.
- **Success fee, banded like tax brackets** (each dollar priced in its
  band, no cliffs): **5%** of the first $1M · **4%** of $1–5M · **3%** of
  $5–10M · **2%** above $10M · **$100,000 minimum** (floor binds below
  $2.25M EV). Worked anchors: $1.8M→$100K (floor) · $3.46M→$148K ·
  $5M→$210K · $10M→$360K · $32.58M→$811.5K (= fixed $360K + 2% over $10M).
- **Every retainer dollar is credited against the success fee at close**
  — prepayment, not a refundable deposit. Credit caps at the fee (a long
  mandate on a floor deal owes $0 at close, excess NOT refunded); **no
  close, no credit** — retainers are earned as the work runs.
- **Premium adds no second formula**: the retainer simply continues
  through integration/value creation; add-on acquisitions run the same
  schedule; one retainer covers the platform.
- SITE PUBLICATION REVERSED (Paul, 2026-08-06, hours after the public
  schedule shipped: "remove the public upfront pricing, what it costs
  section entirely"): NO figure from this schedule appears anywhere on
  the site. The schedule lives in the EMAIL-GATED pricing brochure
  (`content/collateral/smbx-corpdev-pricing.pdf`, delivered by
  `POST /api/practice/pricing` — lead captured first, PDF attached, no
  guessable URL) and in the ENGAGEMENT LETTER. The landing's `#pricing`
  section is ONLY the brochure request (heading + email row); the free
  offering deck (no pricing in it) stays a public download. Yulia and
  the intake still NEVER quote fees. The one-time
  §15(b)(13)/state-registration counsel confirmation remains pending and
  should bless this schedule before the first letter papers it.

## Business Model — The Practice (THE LINE v2)
smbX.ai is a buy-side corp-dev-as-a-service practice. Revenue is per-engagement, papered by humans in the engagement letter: a **buy-side retainer** and a **buy-side success fee**, both paid by the practice's own acquirer client (independent-sponsor economics). The perimeter: buy-side only, one buyer per target, never sell-side/two-sided/neutral-intermediary, targets under $250M revenue. One-time counsel confirmation (Exchange Act §15(b)(13), state M&A-broker regimes, engagement-letter language) is pending — see `THE_LINE_POLICY.md`.

The app itself charges nothing. **Practice mode** (`server/services/practiceMode.ts`, default ON):
- `TEAM_ALLOWLIST` (comma-separated emails; fallback `paulbryantbaker@gmail.com`) gates register/login/Google/reset.
- `practicePerimeter` middleware 403s any JWT belonging to a non-team identity (covers `/api` and `/mcp`, including pre-pivot accounts and external agent keys). Tokenless requests fall through to each route's own auth, so token-link share surfaces keep working.
- Team members get enterprise-level entitlements (`getUserPlan` short-circuit) — no paywall, no free-deliverable gate.
- `/api/chat/anonymous` and `/api/stripe` (except the webhook) return 410.
- **The external agent surface is mothballed (Paul, 2026-07-11):** `/mcp`, agent/MCP discovery (`/.well-known/*`, `/server.json`), the MCP OAuth rail, and the public spec/OpenAPI endpoints return 410 in practice mode (`mothballedAgentSurface`). Code kept intact — reopen deliberately, never by accident.
- The operational core is NOT gated beyond team auth: deals, chat, deliverables, data room, exports/PDF, share links, models, sourcing all run at full capability — these are the practice's working instruments.
- The retired subscription machinery (`subscriptionService.ts`, `lib/pricing.ts`, Stripe envs) stays in the tree, dormant.

## Journeys, Stages, and Deal-Mechanics Gates
Top-level product journeys remain SELL, BUY, RAISE, and PMI. SELL/BUY/RAISE expose six user-facing stages; PMI exposes four post-close stages.

- **SELL:** S0 Intake → S1 Financials → S2 Valuation → S3 Packaging → S4 Market Matching → S5 Closing
- **BUY:** B0 Thesis → B1 Sourcing → B2 Valuation → B3 Due Diligence → B4 Structuring → B5 Closing
- **RAISE:** R0 Intake → R1 Financial Package → R2 Investor Materials → R3 Outreach → R4 Terms → R5 Closing
- **PMI:** PMI0 Day 0 → PMI1 Stabilization → PMI2 Assessment → PMI3 Optimization

The current DEFINITIVE substrate is broader than those stage labels. `server/services/definitiveDealMechanicsCatalog.ts` defines the active v1.1 deal-mechanics catalog: 30 gates and 134 model slots (M101-M234), including G28 Distressed / Restructuring, G29 Capital Structure & Liability Management, and G30 Real Estate & Asset-Class Overlays (which carries the V18c real property & contract law layer, M224-M234 — see methodology/DEFINITIVE_V18C_REAL_PROPERTY_PASS.md; anchor-state law tables live in server/constants/realPropertyLaw.ts). Agents should route through `compose_model_stack`, the route map, and DealState rather than hard-coding only the four journey stage lists.

## Calculation and Model Runtime
There are two layers:

- **Client canvas calculation helpers:** `client/src/lib/calculations/core.ts` contains legacy pure JS helpers for the human interactive canvas: SDE, EBITDA, valuation, DSCR/debt service, SBA financing/amortization, IRR/MOIC, LBO/pro forma, sensitivity, FCF, working capital peg, covenant compliance, tax impact, cap table dilution, exit waterfall, earnout, installment sale, and DCF. This is not the complete substrate catalog.
- **Server V19/DEFINITIVE runtime:** `server/services/v19ModelRuntime.ts` contains the model-backed runtime used by Yulia/agents, currently covering 100+ `MODEL.*.v1` definitions across valuation, QoE, financing, structure, tax, legal economics, real estate, IP, restructuring, capital structure, secondaries, PMI, and research-only overlays. `npm run test:definitive-conformance` currently covers 385 model-runtime cases (655 total), including the 183-case V18c real-property battery.
- **DEFINITIVE deal-mechanics catalog:** `server/services/definitiveDealMechanicsCatalog.ts` is the broader route/discovery catalog with 123 M-slots and 30 gates. Some slots are executable now, some are professional-handoff or research-only, and some are reserved.

## 11 Interactive Canvas Models (client/src/components/models/)
Valuation Explorer, LBO, SBA Financing, DCF, Tax Impact, Cap Table, Sensitivity Matrix, Deal Comparison, Earnout, Working Capital, and Covenant Compliance. These are the human canvas components, not the full agent/substrate model inventory.

## Yulia's Canvas Tools
- `create_model_tab` — opens interactive models from conversation
- `update_model` — modifies assumptions ("what if EBITDA is $1.5M")
- `read_tab_state` — reads canvas state for contextual responses
- `get_sourcing_portfolio` — reads buyer's sourcing pipeline
- SSE canvas_action handler forwards tool results to zustand store

## Reference Documents
- **UI_RETOOL_READINESS.md** — Operating doc for the incoming design-language retool: current surface map, token seams, the preservation contract (LINE touchpoints, agent↔UI vocabulary, locked pricing, funnel), and the phased bundle-landing playbook.
- **YULIA_AGENCY_SPEC.md** — Product/architecture doctrine for Yulia as the agentic operating layer: advisor posture without licensed-advisor boundary crossing, permission levels, surface contracts, data-room/file architecture, and implementation priorities.
- **YULIA_AGENCY_IMPLEMENTATION_PLAN.md** — Practical wiring plan for context packs, prompt governance, governed tool execution, staged approvals, surface actions, Today, Files, and Data Room.
- **SMBX_PRICING_LOCKED.md** — Locked canonical pricing record (2026-05-27): Free / $99 Solo / $249 Pro / $749 Team / $3,000+ Enterprise. If any other doc or code constant disagrees, this file wins.
- **THE_LINE_POLICY.md** — Hard operating policy for pricing, artifacts, Yulia refusals, marketing language, and drift triggers around securities, UPL, tax/accounting/appraisal, brokerage, and transaction-compensation boundaries. Now includes statutory safe-harbor citations (Lowe v. SEC, Tex. Gov't Code § 81.101(c), N.C. Gen. Stat. § 84-2.2) and the "Never Even Accused" four-layer test.
- **DEFINITIVE v1.0** = `methodology/DEFINITIVE_BUILD_PLAN.md` + V19 baseline docs below.
  - DEFINITIVE is the current agent-access/spec/substrate build target as of May 20, 2026.
  - Doctrine: smbX is the M&A diligence substrate; Yulia is the human reference surface.
  - Agent/API/MCP work should use DEFINITIVE naming, spec versioning, Authority Register, beneficial-customer identity, mandate chain, conformance, audit packets, and THE LINE contracts.
- **METHODOLOGY V19 baseline** = `methodology/METHODOLOGY_V19.md` + `methodology/CC_V19_IMPLEMENTATION_BRIEF.md` + `methodology/V19_BUILD_PLAN.md`.
  - V19 remains the current methodology/runtime baseline and rolls forward into DEFINITIVE v1.0.
  - V17/V18a/V18b are archived in `docs/_archive/methodology/` and should not be used as new-build source material.
  - Runtime still contains V18 tax/legal distillations in `server/prompts/taxEngine.ts` and `server/prompts/legalEngine.ts`; V19/DEFINITIVE prompt/runtime migration is tracked in `methodology/V19_BUILD_PLAN.md` and `methodology/DEFINITIVE_BUILD_PLAN.md`.
- **DESIGN_LANGUAGE.md** — THE canonical current visual system (Ledger): tokens, type, shape, logo law, graphics doctrine, voice laws, dead-systems list with hexes. Hand THIS file to any Claude/design tool; it supersedes every other design doc (STYLE_GUIDE.md, CLAUDE_DESIGN_*.md, DESIGN_TOKENS.md, DESIGN_SOURCE.md are all retired with banners, 2026-07-19).
- TESTING.md — Testing tracker with issue template system
- REPO_STATUS.md — Current-vs-stale map for the whole repo (read this when reviewing on GitHub)

## Key File Map
| File | Purpose |
|------|---------|
| client/src/practice/ | The public practice site (`.pd` coral language, v3 Claude Design handoff): Landing, Industries, 5 SegmentPage entries (routed, unlinked), YuliaIntake, PracticeShell, leads.ts — design source bundle at `practiceSite/` |
| client/src/practice/ReportPage.tsx + ReportsIndex.tsx + report.css | PUBLISHED RESEARCH (2026-07-29, Paul: "I want to put my reports on my website… downloadable PDF or a blog post?"). Answer: BOTH, from ONE markdown file. `/research/:slug` renders the full assessment as a readable web page (the destination a LinkedIn post points at — most of that traffic is on a phone, and search can't index a PDF); the PDF is the TAKEAWAY, DELIVERED to the email given (see `server/services/reportAccess.ts`). **The report body is never gated** — only the download is, because the portable file is what gets forwarded to a partner or dropped in an IC packet. Reading affordances a PDF can't offer: sticky contents rail with scroll-spy, progress hairline, and a place-keeper that OFFERS to restore your position (never jumps). `.rp-*` is a self-contained DOCUMENT SCOPE, the same sanctioned exemption `.pd-map`/`.map-*` carries, because it holds parity with the PDF; body ink is the PDF's #3F464C, not the site's #5C6670. Routes are deliberately OUTSIDE the two-surface rule (always public, so a logged-in practitioner checking a posted link sees what a cold visitor sees). Perf law: `Body` is `memo`-ised and its children built in `useMemo` — React 19 compares `dangerouslySetInnerHTML` by prop IDENTITY, so a fresh `{__html}` object re-set innerHTML on every scroll frame (250 kB re-parsed 60×/sec) AND replaced every heading node, silently breaking the scroll-spy (a detached node reports top: 0, so it always reads as "above the line"); the spy also looks headings up by id per frame rather than caching nodes. The canonical path is **`/research`** (renamed 2026-07-29); `/reports` 301-redirects to it and `/reports/:slug/unlock` stays live as a real route, because emailed links point at it. Linked as **Research** in the top nav AND the mobile menu (Paul, 2026-07-29: "I don't see a reports link" — the published assessments are the practice's proof, so they earn a slot in the chrome; this is a deliberate, sanctioned addition to the otherwise-locked v3 IA), plus both footer variants. |
| vite-plugins/report-markdown.ts | Renders `scripts/studio/reports/*.md` at BUILD time — the SAME markdown `build-report.mts` turns into the PDF, so page and PDF cannot drift, and no markdown parser or model reaches the browser. Reads build-report's `<!--cover …-->` convention verbatim (`stat:` → the key-findings band, `accent:` → a framed photo after the matching `##`). Two imports so a listing page never pays for a body: `?report-meta` (title/stats/read time/cover) is imported statically; `?report` (the HTML + contents tree) is imported DYNAMICALLY, so each report's ~50 kB is its own lazy chunk and the masthead paints before the read arrives. Cover/accent/headshot images are emitted as real Vite asset imports (the module id keeps the .md's own directory, so relative specifiers resolve). |
| server/services/reportAccess.ts + migration 108 | REPORT DELIVERY (2026-07-29, Paul, after three passes: "enter the email and have a button 'get the PDF', and the PDF is delivered to that email — that solves both problems"). It does, and it is the neatest form: **delivery IS the verification.** The report READS free on the page; pressing Get the PDF mails it to the address given — the PDF ATTACHED, plus a link in the body for gateways that strip attachments (`sendEmail` gained optional `attachments` and retries once WITHOUT them rather than losing the message). Nothing is released to the browser on submit, so a fake address simply never receives the file and no one is sent on a confirm-your-address errand first. Opening the mailed link marks the address confirmed and sets the signed HttpOnly `smbx_reader` JWT (180d) → `?dl=1`, so a reader we have already delivered to gets their NEXT report in one click. Practice mode restricts real accounts to the team allowlist, so a literal login would have meant nobody but the team could ever download; this is the equivalent that works for an outside acquirer. **Mail is load-bearing:** `issueAccess` reports whether it actually sent and the UI refuses to claim an inbox that will stay empty — set `RESEND_API_KEY` + `EMAIL_FROM` or no reader ever gets a report. (Earlier same-day passes tried magic-link-then-download and download-on-submit; both are superseded.) |
| shared/reports.ts | The report register both surfaces read: slug, kicker, short title, abstract, published date, PDF path, OG image. The CLIENT joins it to the rendered markdown; the SERVER stamps it into index.html for `/research/:slug` (see the injection block in server/index.ts) because LinkedIn/Slack/X don't run JS and would otherwise preview a posted report link as the generic site card. Abstracts are drawn from each report's own executive summary — zero-hallucination law. ADDING A REPORT: drop the .md in `scripts/studio/reports/`, run `build-report.mts` (PDF) + `build-og-card.mts` (preview card) into `client/public/reports/`, add one entry here and one line in `client/src/practice/reports/registry.ts`. |
| scripts/studio/build-og-card.mts | Studio link-preview builder (2026-07-29) — the 1200×630 card a shared report URL previews as. Sibling to build-report/build-deck/build-onepager, reads the same cover config: announcement dark grammar (boardroom texture, white logo, brass kicker, two-tone Fraunces title with the mint turn, ringed headshot byline, framed cover photo). The two-tone split refuses to break on a connector (`&`, `and`, `of`) so a colour run never opens or ends on one. Pure local Chromium — no model, no API key. |
| server/migrations/120_outreach_campaign.sql + server/routes/outreach.ts + house/outreach.ts + crmOutreachSeed.ts | THE OUTREACH MACHINE (2026-08-07, Paul: "let's get everything built out before we seed data … i need to be able to eventually build an automated marketing machine"). The campaign build migration 115 promised: crm_waves · crm_sequence_steps · crm_templates · crm_events, plus **crm_touches — every step's `target_records` expression expanded into per-person work items** (the queue), and `source_key` stamping the plan's record ids onto accounts/contacts so targets resolve by id. The seed now loads ALL seven content/crm-seed tables in one press — nothing deferred; **tested end-to-end against a scratch Postgres: 7 waves · 27 steps · 12 templates · 4 events · 72 touches queued, 11 do-not-pitch excluded, second run a no-op.** The mistake-prone parts are PURE in `house/outreach.ts` (npm run test:outreach, 26 cases): `parseTargets` reads the plan's expressions — ids, "REF-001 through REF-015" ranges, "All CAPITAL records" segment selectors (underscore-guarded prefix match) — and lands anything unreadable in `targetsUnmatched`, NAMED in the seed report, never dropped; `renderTemplate` merges {fields} from the register and REFUSES to blank what it can't fill ({recent_fund_or_deal} stays visible and blocks the send — auto-blanking would mail "congratulations on ." to a Tier A fund). UI = an **Outreach mode on both Clients screens** (desktop `ClientsOutreach.tsx`: queue + compose pane + plan/templates tab; mobile `OutreachM.tsx`: the queue + send/share between meetings). THE LINE, structural: **one touch, one press, one human** — no batch-send endpoint exists; sends go out as a personal-note HTML (no branding) via `sendEmail`, `sent_at` stamped only on a true return (the deal_tasks honest-send law), the final draft denormalised onto the touch (subject_sent/body_sent = the audit trail), and an auto-logged crm_activity email row. Unsubscribed contacts + disqualified accounts are excluded at queue-build AND re-checked at send. The eventual automation layer (worker.ts) may ASSEMBLE batches; it must never release them. |
| scripts/studio/push-crm.mts + `POST /api/crm/import-bundle` | THE COWORK → APP BRIDGE (2026-08-07, Paul: "I need a smart importer that does what you're doing and just puts the data where it needs to go… I'd like to be able to do all of this in Cowork… but I don't want to burn up API"). The division of labour is THE SPLIT's, applied to data instead of documents: the SMART half — reading a messy Google Sheet or a fresh research run, mapping columns, bucketing the layers — happens in a Cowork session on Paul's SUBSCRIPTION; the app-side half is pure code. `seedOutreachFromTables()` (the refactored core of crmOutreachSeed) takes the seven-table bundle in memory, `/api/crm/import-bundle` accepts the CSVs over HTTP (names matched by number prefix OR meaning, unrecognized files RETURNED not dropped, **partial bundles legal** — an updated contacts sheet alone upserts), and `push-crm.mts` is the one-command client (`SMBX_EMAIL`/`SMBX_PASSWORD` or `SMBX_TOKEN`, `SMBX_APP_URL` defaults to smbx.ai). **The endpoint calls NO model** — that is the whole point: the intelligence is the session's mapping, so the loop costs nothing against the metered org key and no data round-trips through GitHub. Job 6 in `content/studio/workspace-CLAUDE.md` carries the laws a session needs (never invent a person to fill a column — a fabricated CRM contact gets EMAILED, which is worse than a fabricated figure; `bucket` decides the layer and mis-bucketing a competitor as a client is how a do-not-pitch firm gets pitched; `tier` is conviction, not size). Job 5 in STUDIO_COWORK.md is the runbook side. **Existing workspaces need `init-workspace.mts <dir> --update` to receive the new laws.** |
| server/index.ts `/api/practice/leads` | Public rate-limited lead capture (practice_leads table, migration 097) + practitioner email ping |
| client/src/components/v6/V6App.tsx | App shell entry — viewport fork into the two Atlas shells |
| client/src/components/v6/desktop/AtlasApp.tsx | Desktop shell — header tab strip, chat rail, canvas tabs, history wiring |
| client/src/components/v6/atlasmobile/AtlasMobileApp.tsx | Mobile shell — Today/Deals tabs, Yulia FAB + sheet |
| client/src/components/v6/atlasmobile/screens/StudioResearchM.tsx | Mobile Studio — Research & campaigns (2026-07-19, Paul: "I need Studio to be available on mobile"): phone-native single-column manager reached via More hub → Studio → Segmented Collateral ⇄ Research (sessionStorage `smbx_studio_mode` shared with desktop; the collateral mode's loading/empty/error states keep the toggle so you can always switch). Folder CHIPS (All · One-offs · per-campaign · Archived · Media · Collateral · Analytics) over RT-token card lists; in-place drill-downs mirror the collateral list↔detail pattern with an inline "Library" back row: run detail (live 3s activity trail, Documents with ?save=1 exports filing into Collateral, REVIEW & approve — editable hooks + REAL authed 1-pager preview (blob AuthImg) + approve POST — manage verbs + file-under select), campaign edit (rename/mandate/format/cadence + Run now/Pause/Archive/Delete), asset preview, analytics detail (verbatim stat grid + top posts + Analyze with Yulia → read rendered via the MarkdownBody now EXPORTED from mobile Studio.tsx). A fresh run auto-opens its live trail once. Mobile niceties: `navigator.share` for post text (paste straight into the LinkedIn app; clipboard fallback), camera-roll photo upload, .xlsx analytics import. Desktop-only by design: drag & drop, campaign groups, the composers, plan import, focal points. |
| client/src/components/v6/desktop/atlasTokens.ts + atlasmobile/mobileTokens.ts | THE app skin knobs (`T` / `M`) — retheme here first |
| client/src/marketing/ | Logged-out marketing surface (`.mkt` scope, MarketingShell + 9 pages) |
| client/src/lib/pricing.ts | Canonical client pricing table — all price UI derives from here |
| client/src/lib/yuliaSurfaceContext.ts | Agent↔UI surface-action vocabulary (preserve across redesigns) |
| client/src/components/models/ | 11 interactive financial model components for the human canvas |
| client/src/lib/calculations/core.ts | Legacy pure calculation helpers for canvas models |
| server/services/v19ModelRuntime.ts | Server-side V19 model runtime for executable/research `MODEL.*.v1` definitions |
| server/services/definitiveDealMechanicsCatalog.ts | DEFINITIVE v1.1 134-slot / 30-gate deal-mechanics catalog (M224-M234 = V18c real property law) |
| client/src/lib/modelStore.ts | Zustand store for model tab state |
| client/src/components/shared/ChatDock.tsx | THE chat input — used everywhere |
| client/src/components/v6/desktop/screens/Sourcing.tsx (+ atlasmobile twin) | Sourcing portfolio UI |
| server/index.ts | Express entry + auto-migrations |
| server/services/aiService.ts | AI orchestration + agentic loop |
| server/prompts/taxEngine.ts | V18 §9 tax foundation + per-league workflow (18a distillation) |
| server/prompts/legalEngine.ts | V18 §10 legal foundation + per-league workflow (18b distillation) |
| server/constants/v19Regulatory.ts | V19 regulatory constants for HSR, SBA, OBBBA, §382, CFIUS, RWI |
| server/constants/v19Leagues.ts | V19 L1-L10 league classification constants |
| server/services/sourcingPipelineService.ts | 5-stage sourcing engine |
| server/services/researchAgent.ts | Internal research agent (2026-07-15): 6 research types × 3 depths, server-side web_search/web_fetch via Sonnet 4.6 (STREAMED pause_turn resume loop — non-streaming hit the 5-min header wall and died "Request timed out", 2026-07-18), cited md report + STUDIO FEED, cents-tracked spend vs monthly cap, in-process campaign scheduler (weekly Sun 22:00 UTC / biweekly / monthly, completion email); live ACTIVITY trail (migration 102 `activity` JSONB — every search/read/phase pushed as the run executes, kept as the run's record) |
| server/services/researchComposer.ts | Research + announcement artifacts in the CORAL language (not terra premium-PDF skin): letter report PDF, 1080×1350 LinkedIn card PNG, LinkedIn doc-post PDF, and the announcement card (light/dark, media-library photo + focal crop) — rendered on demand via shared Puppeteer; real logo in the site's two treatments (coral as-is / white-inverted on dark). HEADSHOT LAW + findings-first (2026-07-19, Paul: report must be postable RESULTS, collateral was "boring", "I need my headshot on anything produced"): `authorPhoto()` pulls the headshot from studio_assets (label /headshot/i, focal crop; 60s cache; disk founder-portrait fallback; initials disc last resort) and `faceImg()` stamps it on EVERY artifact — report byline, 1-pager dark textured footer (mint-ring face + name), carousel cover byline + closer face. Report PDF = RESEARCH FINDINGS register: brass KEY FINDINGS stat block ABOVE the body, byline under the title, sources kept, the internal STUDIO FEED appendix REMOVED from the shareable PDF (hooks/angles live in the review sheet only), shareable disclaimer. 1-pager: the LEAD cited data point renders as the site's signature giant numeral with a brass bar (the number IS the story graphic); remaining points as dot rows. STORY ARTWORK + carousel design pass (2026-07-20, Paul: covers/pages "boring just text", plan-Claude specifies an accompanying visual): `server/services/artworkService.ts` renders the feed's `visual` brief into a flat Ledger-palette editorial illustration via Gemini image REST (`GEMINI_IMAGE_MODEL` env, default gemini-2.5-flash-image; STRICTLY fail-soft — any error logs one activity line and the deck falls back; prompt bans text/people/logos/charts in-image) filed into studio_assets with run/schedule provenance (label "Artwork — …"); hook runs at completion for non-report runs when GOOGLE_AI_API_KEY is set, with trail entries. Carousel pages: cover = art panel (generated artwork via `runArtwork()`, else the SECTOR ILLUSTRATION) beside the hook + byline; light pages carry ghost Fraunces page numerals + a low-opacity sector echo; the TAKEAWAY page flips to the dark boardroom band (brass FOR THE ACQUIRER tag); stat pages get the brass bar. Plan importer folds any per-slot visual spec into the mandate as "Visual direction:", and the feed prompt honors it in `visual`. SECTOR ART + LIGHT MATERIAL + POSTER PASS (2026-07-20, Paul rejected the abstract motifs — "find visuals that represent the industries and verticals we talk about… we need a light version of the dark texture… look at movie art"): `server/services/sectorArt.ts` = 21 hand-drawn line-art trade illustrations (ink/green/brass, 400×400 grammar with ground line — extinguisher, elevator doors, transmission tower, thermostat, weld+magnifier, drum+wand, water tower, warehouse racking, gear+turned part, conveyor jars, NEMT van, invoices, HVAC RTU, pipe+valve, breaker panel, mower+tree, gable+ladder, semi, server rack, clinic, skyline+crane) keyword-matched from topic+title (hunt-board verticals first, `default_building` fallback); `motifSvg` abstract geometry DELETED. Light pages/card carry `/textures/bonebleed.webp` (the blackbleed master inverted+warmed to bone in a one-time Chromium bake) as material. Movie-poster construction on the cover: PROOF STRIP of the top cited stats with mono attributions where a one-sheet stacks review quotes, TWO-TONE hook (`twoToneHook` — first beat ink, the turn deep green, on card + cover), and a mono CREDITS strip ("RESEARCHED & WRITTEN BY PAUL BAKER · … · EVERY NUMBER CITED"). ANNOUNCEMENT-TEMPLATE LAW (2026-07-20, Paul saw a live deck: "the background is ugly… stick to the original announcement styles as templates… the original announcements are great, keep those, as either single one-pager or multiple page pdf" — supersedes the same-day poster full-bleed + bonebleed texture, both REMOVED): the 1-pager AND the carousel cover are the announcement light-split (left editorial column: logo+kicker, two-tone Fraunces hook, rule, sub, coral-dot data bullets w/ mono sources, mandate line from the takeaway; right white panel: picked artwork else the sector illustration with a WARM edge fade; flat dark 128px footer: white logo + ringed headshot byline, cover adds mint SWIPE). Light content pages are FLAT bone + the announcement wash with a slim 84px dark base (white logo + n/total); stat pages keep the giant numeral + brass bar; the dark textured takeaway + closer stay (Paul: "the dark texture is cool"). bonebleed.webp is unused (asset kept on disk). `runArtwork(run)` honors the REVIEW SHEET pick via merged `feed.artAssetId` (undefined=auto this-run generated art · null=none · number=that media asset, ownership-checked in the feed PATCH); ReviewPanel gains a Cover-artwork thumbnail picker (None + generated + media photos, blob-authed) with Generate/Regenerate (POST /research/runs/:id/artwork — regenerates from feed.visual or a custom brief and sets the pick) and a second live preview: the carousel cover (GET /research/runs/:id/cover.png via `renderCoverPng`). BOOKEND LAW v3 (2026-07-21, Paul, 3rd day: "the darks are together and do not actually bookend the story" — supersedes v2's takeaway+closer tail pair): EXACTLY TWO dark pages — the COVER (front) and ONE merged CLOSER (back). linkedInDocHtml pulls the takeaway out of the page loop (`bodyPages` = non-takeaway) and FOLDS it into the closer (`.closer-take`: FOR THE ACQUIRER brass tag + Fraunces payoff head + mint rule + body + ringed headshot byline + white logo + FOLLOW), so the deck never ends on a takeaway+closer dark pair. Cover = the announcement DARK (`.cover-dark`): boardroom band, ivory two-tone hook (mint turn), left column now vertically CENTERED via `.cv-body` (flex-1 justify-center; `text-wrap:balance` on the hook) with the kicker pinned top; the cover image BLEEDS full-height on the right and its edges DISSOLVE into the dark via `.cv-imgfade` (left+bottom+top DARK→transparent gradients — no hard card/shadow, Paul: "the picture is not formatted in or faded in nice"). CAROUSEL PREVIEW (same day, Paul: "I don't see how to preview the carousel before I save… I see only the first and the last pages"): `renderCarouselStripPng` full-page-screenshots the whole doc into one tall PNG (GET /research/runs/:id/carousel.png); the desktop ReviewPanel's second preview is now "Carousel · every page (scroll)" in a maxHeight scroll box (replacing the cover-only preview), 1-pager relabeled "single-image post". The 1-pager card stays the light announcement (style A). FONT EMBED LAW (same day, Paul's production PDFs rendered typewriter-mono): `fontEmbeds.ts` inlines Fraunces/Inter/IBM Plex Mono woff2s (@fontsource deps) as base64 @font-face into the shared FONTS head — render paths must NEVER depend on the Google Fonts CDN (Railway blocks it; Docker has only Noto). RENDERER-PROOF PASS (2026-07-21, Paul round 3: "the issue is the way the PDF is rendered when it is opened by Preview or when the LinkedIn previewer shows it"): the carousel PDF is RASTERIZED — renderLinkedInDocPdf screenshots each finished page at 2x (JPEG q92) and rebuilds the PDF as one flat image per page, because page.pdf()'s vector objects + soft-masked gradients get seamed/banded by Preview and LinkedIn's rasterizer (white hairlines at object edges) while Chromium's compositor is what the previews show. Same pass: images on LIGHT surfaces sit in FRAMED PANELS (1-pager .right inset hairline card radius 24; carousel .split-r framed — the warm dissolves are dead: fades on light paper read as smudges, Paul: "a border box that can hide the rough edges"), the dark cover's flat cv-imgfade became cv-texfade (a full-section replica of the boardroom texture+halo dissolved with a CSS mask so the WEAVE melts across the picture — a flat gradient beside texture prints a cliff), deckDesigner prompt v4 carries the two-treatment law + baked-in-gradient awareness, and artworkPrompt bans baked-in vignettes/edge fades (uniform flat background to all four edges). ON-DEMAND artwork: `ensureRunArtwork` generates at first download when no art + no explicit none-pick + GOOGLE_AI_API_KEY (feed.visual else `derivedVisualBrief(title)` — pre-pipeline runs self-heal); POST /studio/artwork = the announcement composer's standalone Generate (brief else headline-derived). LINE-ART RETIRED (2026-07-20 late, Paul on the sector illustrations: "awful… looks terrible"): sectorArt drawings render on NO artifact anymore — the cover/1-pager panel priority is story artwork → `brandPhoto()` (non-headshot library photo, walking shot preferred, disk founder-walking.webp fallback — the announcement original's photo grammar) → centered logo block; sectorArt.ts stays on disk, unreferenced by the composer. PER-PAGE ARTWORK (2026-07-20 night, Paul: "drag and drop from the media list onto which page i want that image… then save"): feed.pageArt {finalPageIndex: assetId} rides the review PATCH (ownership-checked, full-replace map); docPages stamps it post-bookend (cover excluded — feed.artAssetId governs the cover); stat/story pages carrying an image render the announcement text-left/image-card-right SPLIT (numeral capped, image card 380×660 radius 24); GET /research/runs/:id/pages returns the final page order for the review sheet's PAGE TILES — desktop ReviewPanel: artwork thumbs are drag sources, page tiles are drop targets (takeaway tiles locked — the photo-block law), × clears, pageArt saves with Save & re-preview, and the dropped images ship in the Carousel PDF (loadPageArt in renderLinkedInDocPdf). |
| server/services/deckDesigner.ts | CLAUDE-DESIGNED CAROUSELS (2026-07-21, Paul after 3 days of template fights: "you're trying to hard code the builder instead of letting yulia / claude do it the way you would do it… the hard-coded builder can't do it"): Paul assigns copy + which Media image goes on which page (review sheet unchanged); CLAUDE composes the actual pages — Sonnet (RESEARCH_DECK_MODEL) SEES every assigned image via vision blocks and returns ONE <style> + N <section class="pg"> (1080×1350) under a locked brand contract (Ledger palette/type tokens {{LOGO}}/{{LOGO_WHITE}}/{{HEADSHOT}}/{{TEXTURE_DARK}}/{{IMG_n}} substituted post-generation — the model never sees base64 out, never emits remote URLs). HARD VALIDATION or template fallback (never an error): page count, exactly-two-darks prompt law, tokens known, no http srcs, headings/stats/sources VERBATIM (entity/quote-normalized) + every body number present. Cache on the run (feed_override.deck {key,html}, key = sha1 of inputs+model+prompt version) so previews match exports and saves auto-invalidate; in-flight dedupe across the 3 render endpoints; review sheet gains "Redesign deck" (POST /research/runs/:id/deck clears cache). designedDeckHtml (researchComposer) feeds renderCoverPng/renderCarouselStripPng/renderLinkedInDocPdf; the fixed template survives ONLY as the fail-soft net. Vision thumbs downscaled via shared Chromium canvas (no native deps). |
| server/services/studioAssets.ts | Studio media library (migration 099 studio_assets, bytea in PG): upload/list/serve/patch/delete + per-asset focal point that drives object-position framing in every composer; lazy-seeds the two founder photos |
| server/services/studioRepos.ts | THE THREE STUDIO REPOSITORIES (2026-07-24, Paul: "just like in Google Drive, we need repositories for different types of data"). **ARTIFACTS** = the words (reports, LinkedIn post copy, research masters, captions, deck specs) — the text a piece of collateral is generated FROM; **ASSETS** = the pictures used in them; **COLLATERAL** = the finished output posted on LinkedIn or sent to a client. Assets/collateral already lived in `studio_assets` split by `kind`; artifacts had NO home (report md was scattered across `research_runs.report_md` and `research_lanes.master_md`, unbrowsable and un-editable), so migration 109 adds `studio_artifacts` (kind · label · body_md · notes · archived + provenance lane_id/lane_version/run_id/schedule_id) and `studio_assets.artifact_id`, closing the chain **ARTIFACT (words) + ASSET (pictures) → COLLATERAL (output)** so a finished PDF can answer "where did this come from". API: `/studio/repos` (counts), `/studio/artifacts` CRUD, `POST /research/runs/:id/file-artifact` and `POST /research/lanes/:id/file-artifact` (re-filing a lane version UPDATES its artifact instead of duplicating). UI: the desktop Studio sidebar's old "Assets" section became **Repositories** — Artifacts · Assets (the row formerly labelled Media; `sel.kind==="media"` unchanged) · Collateral — with a Kind column, New artifact / Upload .md toolbar, and a plain-markdown inspector editor (label + kind + body, Save/Download .md/Copy/Delete, dirty + Saved states, full body fetched on selection since the list carries only a 240-char preview); mobile Studio gets the same as an Artifacts chip + read/edit detail (share text via `navigator.share`). Both run inspectors gained "File in Artifacts". |
| server/services/researchLanes.ts (+ Studio sidebar "Research lanes") | THE LIVING MASTER DOCUMENT (2026-07-24, Paul: "I'm gonna run the research in Claude Web app, I will run a research and Gemini probably or other tool just so we have a variety of research… then I will upload all of the research for it to pass through and calculate all the information into one final document… I want to do this part in the app too so that I can keep all the research in the database and keep it updated" — refreshed "maybe once a quarter"). Migration 108: `research_lanes` (master_md + master_version + synthesis_status) · `research_sources` (PDFs as bytea, markdown as text, `incorporated_version` marks what's been folded in) · `research_master_versions` (every version keeps its `audit`). CITATION LAW IS MECHANICAL, not requested: `auditCitations()` checks that every figure appears in a source OR is registered in a Derivations section with its working, that every uploaded document is acknowledged in the Sources register, and that source URLs survived; conflicting sources keep BOTH values (a range citing both endpoints passes, an invented midpoint does not); a failing draft is retried ONCE with the violations named, kept only if it improved, then SAVED but parked `needs_review` (never lose the work, never let it go out silently). 27 test cases — three exist because the check was wrong first ("$600 and $700 million" read as a bare $600; bare `$50.7B` unmatched against "$50.7 billion"; a dropped source passing because its topic words appeared in the body title). UI (2026-07-25 — the backend shipped without one): sidebar section above Repositories, "+ Lane" creates one, folder toolbar has **Upload research** (PDF/.md) and **Paste research** (text + which tool produced it), the list shows sources (green = folded into v_n_, amber = pending; × removes) then master versions, and the inspector runs the synthesis — "Fold in new research" / "Rebuild from all", the running state, the `needs_review` banner carrying the actual audit note, **File in Artifacts** (how a master becomes collateral) and Download master (.md). Synthesis is a LONG in-process pass, so the POST may outlive the proxy: the lane ROW is the source of truth and the client POLLS (a dropped response is not a failure); `synthesizeLane` refuses a concurrent run but takes over a `running` lock older than 30 minutes, so a deploy mid-synthesis can't wedge a lane forever. |
| client/.../screens/MarketWorkspace.tsx + server/services/corpDevDocs.ts + marketKnowledgeTools.ts | THE MARKET IS THE WORKSPACE (2026-07-26, Paul rejected the Finder IA: "i dont think i like the process at all from how it looks. i need to: 1 - Add all research and artifacts, 2 - synthesize and compile, 3 - create collateral, 4 - do other work — market maps, who's who, thesis building or other corp dev beginning work"). Folders ask WHERE something goes; he asks WHAT STEP he is on. Studio now opens on a market and reads left to right — **Research → Master document → Produce** — with the old Finder surviving as an "All files" tab (view persists in localStorage `smbx_studio_view`). **Step 4 is the reframe:** a lane was implicitly a marketing-content pipeline; it is really the KNOWLEDGE BASE FOR A MARKET, and LinkedIn collateral is one thing you draw off it. Steps 3 and 4 are the SAME mechanism (read the master, produce a document), so they render as two groups of one panel — Corp dev above Collateral. `corpDevDocs.ts` = three client-facing documents (**market map · who's who · investment thesis**), each an ARTIFACT derived from the master so it edits by hand, renders through the report builder, and seeds collateral like any other artifact. Their prompts carry THE LINE harder than anywhere else in Studio (buy-side only · no specific-target valuations · no unlicensed opinions — name the specialist instead · no fee talk · nothing the master doesn't say) and each ends on "What we don't know yet"; `figuresNotIn` audits the output, retries ONCE naming the offenders, keeps the retry only if it improved, saves either way with survivors surfaced. **ONE MASTER, ONE PLACE (same day, Paul: "when we complete a master research — that can be updated later — where does it go?"):** the master lives in `research_lanes` and is NEVER copied — `composeFrom({source: {kind:'lane'|'artifact', id}})` reads it live, so re-synthesizing to v4 can't leave a stale v3 snapshot behind and there is no "which one is real". "File in Artifacts" is gone from the lane UI; the master is READ-ONLY in the app (hand-editing would silently invalidate what the audit claims) and the DERIVED documents are the editable layer. Migration 111 scopes runs + campaigns to a lane; **assets and analytics deliberately stay global** (a photo of Paul is used across every market; a LinkedIn export is account-wide). **YULIA READS THE MARKETS** (`marketKnowledgeTools.ts`, 3 tools folded into TOOL_DEFINITIONS + dispatched ahead of the deal tools): `list_markets` · `read_market` (headings first, then one section — a master is tens of thousands of words) · `search_market` (passages WITH surrounding context so a figure keeps the sentence that sources it; zero hits is the honest "the research doesn't cover it"). Every response carries the master's version + freshness, escalating to a loud "LAST UPDATED n MONTHS AGO" past 6 months, and flags a `needs_review` master before its figures get quoted. This is the NotebookLM comparison Paul drew, closed: interrogate your own research. **Known limit: the audit checks NUMBERS, not prose — a fabricated qualitative claim carries no figure and passes.** |
| server/services/collateralComposer.ts + client/.../screens/CollateralBuilder.tsx | THE COLLATERAL BUILDER (2026-07-25, Paul: "after a master document is created… from the master document I need to be able to select which output type I want — a one pager, a PDF carousel, or a full depth PDF report. And then once the pages are rendered in preview I need to be able to drag drop whichever images or assets on which pages, all on one screen"). **A composition IS a research_runs row** with `origin='composed'` + `artifact_id` (migration 110) — no parallel entity, so every existing render/preview/pageArt/`?save=1`-into-Collateral endpoint applies unchanged and there is exactly ONE renderer. `composeFromArtifact()` turns the artifact's markdown into a STUDIO FEED through `composeFeedFromMarkdown()` (researchAgent's OWN feed prompt, exported verbatim — a master IS a report to that step, and a second prompt would be a second place to drift), then CHECKS it: `figuresNotIn()` (researchLanes, same FIG_RE/range-expansion/normalisation as the master audit, 8 new test cases) flags any figure on a card that isn't in the document, retries once naming the offenders, keeps the retry only if it improved, and surfaces survivors as a citation banner in the builder. Recomposing the same artifact+type UPDATES the row and PRESERVES the page layout (pageArt/artAssetId out of feed_override) while dropping stale copy edits + the cached deck. Board: `GET /research/runs/:id/board` returns the ACTUAL RENDERED pages as JPEG data URIs + drop metadata — carousel via `renderDeckPageThumbs` (one Chromium session, screenshot each `.pg`), 1-pager via `renderCardThumb`, report via `renderReportSectionThumbs` (slices anchored at each `h2`). Report drops attach to SECTIONS, not printed pages (`feed.reportArt` → framed `.rb-accent` band under that `## ` header, the build-report.mts `accent:` grammar; `numberSections()` stamps `id="sec-N"`); carousel drops stay `feed.pageArt`. `houseDeckHtml` now REMAPS a page carrying an assigned photo to the house `trade` layout (text left, framed image panel right) — the plain numeral/statement templates have no image slot, so without it a dropped photo vanished whenever the Claude designer wasn't the path taken. UI = a full-frame takeover inside the Studio frame (absolute, never fixed — Safari rule) opened by "Build collateral →" on any artifact: output segmented control · rendered page board (drop targets, click to zoom) · Assets rail (drag sources) · Apply images & re-render · Export to Collateral. Apply is one explicit button because placing an image changes the deck key and re-runs the designer — not a save per drop. |
| server/services/linkedinAnalytics.ts | Studio Performance (2026-07-19, Paul: "import LinkedIn campaign analytics, have Yulia analyze… and suggest improvement plans"): dependency-free XLSX reader (zip central directory + worksheet XML + sharedStrings — no new deps) parses LinkedIn aggregate exports (DISCOVERY/ENGAGEMENT/TOP POSTS/FOLLOWERS/DEMOGRAPHICS) MECHANICALLY into `studio_analytics` (migration 107) — zero hallucination, every stored number is verbatim from the export (verified against Paul's real 2026-07-13→19 file); `analyzeLinkedInImport` = Yulia's read (Sonnet 4.6 streamed, NO web tools, prompt hard-forbids numbers/benchmarks not in the data, THE LINE + employer-anonymization + [YOURS:…] placeholder law baked in, Studio runs/campaigns passed as context) → fixed-section md report (what happened/worked/didn't · who's watching · per-slot next-week plan in the six-format vocabulary with visual pairings · falsifiable experiments); routes: POST/GET/DELETE /research/analytics(+/:id), POST /:id/analyze (async, status on row) |
| server/routes/research.ts | Research runs/schedules CRUD + pdf/card.png/doc.pdf/post.txt downloads + /studio/assets CRUD + /studio/announcement.png compose (behind blanket /api auth + practice perimeter) |
| studio-kit/ | STANDALONE STUDIO KIT (2026-07-22, Paul chose the lean kit over cloning the 471MB app). A self-contained copy of the deck builder — own package.json (~5 deps: puppeteer-core, tsx, 3 @fontsource), vendored `brand/` assets (logo x2 + blackbleed texture ONLY — Paul, 2026-07-22: "get rid of the copied ones"; the copied founder-portrait/founder-walking photos were removed so the kit ships NO photo of Paul), `lib/render.mts` (puppeteer-core launching the machine's own Chrome — auto-detected on macOS, no download) + `lib/fonts.mts` (embed the woff2s), the builder + init-workspace + example + posting-plan + README. The byline headshot now comes from the deck spec's `headshot` or a `headshot.{jpg,png,webp}` the user drops in ./media (auto-found, quiet); no headshot → a "PB" initials badge, never a stock face. NO imports from server/ or client/ — copy the folder anywhere, `npm install`, `npx tsx build-deck.mts decks/<name>.deck.mts`. This is the retire-the-app end state; the `scripts/studio/` copy below stays for in-repo/cloud-Cowork use. |
| scripts/studio/build-deck.mts | LOCAL DECK BUILDER (2026-07-22, Paul: "move from App to my computer" after the Anthropic org monthly cap). A standalone CLI that renders a house-style LinkedIn carousel (Ledger palette/fonts/texture, framed-cover grammar, rasterized renderer-proof PDF) from a plain `deck` spec — pure local Chromium, ZERO model cost, no app key. Imports only `fontEmbeds.fontFaceCss` + `premiumPdfRenderer.newRenderPage`. LOCAL-FIRST defaults: images resolve against `./media` then `./assets`, output writes to `./collateral` (all overridable with `--media`/`--out`). `npx tsx scripts/studio/build-deck.mts <spec.deck.mts>` → `<slug>.pdf` + page JPGs + caption. Page kinds: numeral · statement · diagram (cover + closer are auto dark bookends). Worked example + field reference: `scripts/studio/decks/elevator-teardown-1.deck.mts` (D02, renders from repo assets standalone). |
| scripts/studio/build-onepager.mts | STUDIO ONE-PAGER BUILDER (2026-07-22, Paul: "make it repeatable and robust" — sibling to build-deck.mts, owns single-image 1080×1350 LinkedIn posts: availability / statement). Spec-driven (`export const post = {...}` — kicker/numeral/numeralLabel/hook/body/invite/cta/byline/image/imagePos/variants/caption), renders **dark + light** PNG + renderer-proof single-image PDF + caption via local Chromium (no app, no API key). The approved vertical split: copy column (dark boardroom or light bone) + full-bleed photo with a recessed-shadow seam (no white line, any renderer); omit `image` for a full-width text card. Image resolver adds repo `client/public` so founder/brand photos resolve by bare name (`founder-walking.webp`); use a non-breaking hyphen ‑ (U+2011) in the hook to glue compounds like buy‑side. THE LINE: invite-never-dare, no fees, no invented numbers, Paul's real photo. Worked example + field reference: `scripts/studio/decks/2-open-seats.post.mts`. |
| scripts/studio/build-report.mts | STUDIO REPORT BUILDER (2026-07-23, Paul: "a full report, not just a carousel… format it the same, I just don't want to lose any fidelity of information" — sibling to build-deck.mts/build-onepager.mts, owns LONG-FORM multi-page research reports). Renders any plain markdown (headings, GFM tables, lists, bold/em) into the Ledger language: dark boardroom title cover (white logo + brass eyebrow + Fraunces title + brass rule), brass part-rules on `#` headers (page-break-before), Fraunces `##`/Inter `###`, hairline GFM tables (mono uppercase headers + brass underline + tabular-nums), green list markers, `format:'Letter'` with a mono page-number footer — pure local Chromium (no app, no API key). Convention: everything before the FIRST `---` is the cover (lead with one `# Title`); everything after flows as the body. Imports only `fontEmbeds.fontFaceCss` + `premiumPdfRenderer.newRenderPage` + `marked`. RICH COVER (2026-07-23, Paul: logo was stretched, wanted his headshot as owner + cards to frame the info): the dark cover carries an optional `<!--cover …-->` config comment atop the .md — `byline:`/`role:`/`headshot:` (bare name, abs path, or beside the .md; defaults to the repo founder portrait) + repeatable `stat: VALUE | LABEL` lines → a brass-numeral "by the numbers" card band; the cover's numbered list auto-renders as framed brass-tab cards; an owner byline (mint-ringed headshot disc) pins to the bottom. Logo fix: `.cv-logo` gets `align-self:flex-start` so the column flexbox stops stretching the 4:1 mark. Absent config → a plain cover still works. COVER IMAGE + ADAPTIVE PAGINATION (2026-07-23, Paul: "i do still want a cover page image like the carousel has"): the cover config also takes `image:`/`imagePos:` (resolved like the headshot) → a framed hero band sits under the title, the carousel-cover grammar in the report format (renderer-safe framed panel, no soft-mask dissolve); an optional `## ` subtitle rides beside the `# ` title; a `footer:` config (or the title) sets the running-footer label; repeatable `accent: <heading substring> | <img> | [pos]` lines drop a framed photo band right after the matching `## ` section header (renderer-safe framed panel on the bone page, like the cover); and pagination is adaptive — page-break before `# ` parts if the body has any, else before its top-level `## ` sections (some reports lead with `##` and never use `#`, e.g. the MEP assessment). `npx tsx scripts/studio/build-report.mts <report.md> [--out <dir>] [--slug <name>] [--eyebrow "…"] [--footer "…"]` → `<slug>.pdf`. Worked sources: `scripts/studio/reports/home-services-master-assessment.md` (# parts) and `commercial-mep-buy-side-assessment.md` (## sections, cover image + subtitle). |
| house/audit.ts + scripts/studio/audit.mts | THE CITATION AUDIT, PURE (2026-07-26). `auditCitations`/`figuresNotIn` were extracted out of `server/services/researchLanes.ts` (which imports Postgres at module load, so a local CLI could not touch it) into `house/audit.ts` — zero deps, no db, no API; researchLanes re-exports it so the app and the local CLI run the IDENTICAL check. `audit.mts` is the local front end: with no flags it audits a document against a sibling `research/`/`sources/` folder (the workspace layout, so the common case needs no arguments). Two honesty guards learned the hard way: a document filed beside its own sources would audit against ITSELF and return a meaningless clean bill (byte-identical sources are excluded), and PDFs have no text layer here so they are COUNTED AND NAMED as unaudited rather than skipped — a clean bill that ignored half the sources is worse than none. Exit codes 0 clean · 1 not clean · **2 NOT AUDITED** (the third state: no machine-readable source at all, which is not the same as passing). |
| scripts/studio/thesis.mts | THE THESIS REGISTER (2026-07-27, Paul: "let's create the thesis builder for any of our verticals and save it such that Cowork can access it any time as we may have multiple thesis for the same markets"). A thesis is not a document, it is a **position** — held *for a particular buyer* (a family office and an independent sponsor underwrite the same market differently), and it **ages as the master beneath it moves**. So a market carries one thesis per buyer profile (`documents/thesis-family-office.md`), each opening with front matter naming `market · profile · master_version · date · status`. **`master_version` is the load-bearing field:** `staleness()` compares it against the version computed from `versions/master-vN.md` on disk, which makes going stale a fact on disk instead of something to remember — re-synthesize to v3 and every thesis still stamped v2 says so out loud. Commands: `new <market> <profile>` (scaffolds, stamped with the current master version) · `list` · `check` (exits 1 if an ACTIVE thesis is behind — drafts don't ship, so they don't fail the gate) · `register` (rewrites `THESES.md` at the workspace root: a standing table plus an assumption ledger pulled from each thesis's "What has to be true" via `[x]/[~]/[?]/[ ]` markers — the untested ones ARE the work list). Diagnoses "rests on nothing" (market has no master) ahead of "no provenance" (master exists, stamp missing), and flags a thesis claiming a version that does not exist rather than reading it as current. Pure fs, no API, no db. |
| house/screen.ts + scripts/studio/screen.mts | THE TARGET SCREEN, LOCAL (2026-07-28, Paul: "this whole search and rank function needs to be built locally so that Cowork can do it - using a google Sheet is likely the best way to manage big lists and rankings"). Ports the app's 5-stage sourcing engine to the workspace, minus the two parts that could not survive: the app decided "is this independent?" **nowhere at all** (grep confirms — `franchiseMatchingService.ts` is buy-a-franchise matching, unrelated), and estimated revenue by asking Sonnet to guess from Google review counts (`deepAnalysisPrompt.ts`: "<10 reviews = <$500K, 10-50 = $500K-$2M…") — an uncited figure that fails the citation law everything else now runs on. So: **affiliation is a REGISTER LOOKUP, not a judgement** (`screen/consolidators.md`, written from the master's who's-who; match on brand-in-name or website domain, never phone — franchise locations carry local numbers, and a miss that looks like independence is the expensive error), and **revenue is a BAND with its arithmetic attached** (employee range × NAICS revenue-per-employee, `revenue_basis` emitting a ready-made `## Derivations` entry naming both assumptions; no benchmark or no review count returns NO band rather than a guess). Scoring is adapted not copied — the app's succession/certification/recurring-revenue weights needed a Haiku call per candidate, so they are gone; **affiliation GATES rather than weights** (an owned business scores 0, an unverified one takes a visible ×0.85 — scored as a 25-point bonus in the first cut, a two-review shop with no website still cleared tier C on independence and geography alone). CLI: `init` seeds screen.md (buy-box front matter + Queries × Geographies matrix + review→employee proxy), consolidators.md and benchmarks.md · `pull` fires the matrix at Places text search (ID-only mask = free, paged) then Place Details ($17/1k, first 5k/month free, count printed before it spends, `--max`/`--yes` guards) · `rank` is free and offline. The board is `screen/candidates.csv` because Paul manages it in Google Sheets — `toCsv` preserves every column he adds there across a re-rank. `GOOGLE_PLACES_API_KEY` is a different key from the Anthropic one, which is the point: the search was never what the spend cap broke. **RETENTION (2026-07-28, Paul: "how does using the Places Key work if i cannot store data?" — a defect in the first cut, and the app has it worse: `sourcing_candidates` persists Places content in Postgres forever under a client header claiming "cache 24 hours max" that nothing enforces):** the terms let you keep PLACE IDS indefinitely but treat name/phone/rating/reviews as a temporary cache, so rows carry `fetched_at`, `rank` warns past `CACHE_DAYS` (30), and `refresh` re-pulls them or `--forget` clears them — `PLACES_CONTENT` names the borrowed columns and `forgetContent()` keeps the place ID, the user's own columns AND the affiliation/score judgements (those are Paul's analysis, not Google's data). The doctrine line, carried in every doc: **Places is DISCOVERY, not evidence** — verify against the licence registry or the company's own site before a name reaches a client document, and cite that. 57 test cases in `house/__tests__/screen.test.mts`; **two exist because the first run got it wrong** — the seeded consolidators.md carried a placeholder `## Example Platform Co` that parsed as a live entry, so the register looked populated and a Neighborly franchise ranked #1 tagged "independent" (parseRegister is now fence-aware and the template's example is fenced, so an unfilled register parses EMPTY → everything `unknown`). |
| scripts/studio/init-workspace.mts | One-command LOCAL workspace setup: `npx tsx scripts/studio/init-workspace.mts ~/smbx-studio` creates the whole system of record on Paul's disk — `markets/<m>/{research,master.md,versions,documents,collateral}` + `deals/<d>/{documents,analysis}` (each seeded with an `_example-` folder + README so the shape is shown, not described) + media/ assets/ collateral/ decks/ + posting-plan.md. Critically it also copies **`content/studio/workspace-CLAUDE.md` → `CLAUDE.md`** and **`content/studio/PLAYBOOK.md`** into the workspace, so a Cowork session opened on that folder picks up the laws and the document specs automatically — with the app out of the loop, nothing else enforces them. |
| content/studio/workspace-CLAUDE.md + PLAYBOOK.md | THE LAWS AND THE SPECS, TRAVELLING WITH THE WORKSPACE (2026-07-26/27). `workspace-CLAUDE.md` lands as the workspace's own `CLAUDE.md`: the layout, the four jobs (fold research into a master · derive a corp-dev document · produce collateral · deal analysis), and the laws that used to live in server prompts — citation law (**a rounded figure is a different figure**; conflicting sources keep BOTH values, never an invented midpoint; and the honest caveat that the audit checks NUMBERS, not prose), THE LINE, attribution + employer anonymization, real-photos-only, voice. `PLAYBOOK.md` is the document specification: **market map · who's who · target map · investment thesis**, each with its section structure. The target map is split into a **draft board** (when target-level research exists) versus a **screen specification** (when it doesn't) — because a market master contains no target list, and building one from it invents companies, which is the worst failure mode this practice has. **`server/services/corpDevDocs.ts` is now a stale duplicate of this file** (it lacks the target map entirely) — PLAYBOOK.md is canonical; retire corpDevDocs.ts when the app's Studio comes out. |
| content/studio/FORMATS.md | THE COLLATERAL SPEC (2026-07-28, Paul: "Cowork keeps drifting on the style templates… it's creating a report, but it's not doing exactly the same as as you have been creating" + "we need to also understand what imagery we need Gemini to create… every time"). Two symptoms, one cause: a session that does not know the container dimensions neither asks Gemini for the right aspect nor places the result correctly, so it improvises HTML to "match the style" and the output drifts. Four laws: (1) **never hand-roll a layout** — the three builders are deterministic, so a different-looking output means a wrong SPEC, not a wrong renderer; (2) **only certain slots take an image** — `trade` is the ONLY carousel body page with an image slot and an `image:` key on `numeral`/`statement`/`diagram` is SILENTLY DROPPED (the build succeeds, the picture is gone — and the example spec's own field reference omitted `trade` entirely, which is the root cause, now fixed in `decks/elevator-teardown-1.deck.mts`); (3) the **exact slot table**, measured from the CSS rather than remembered — carousel cover 476×1102 (ask 9:16), carousel `trade` 404×604 (ask 3:4), one-pager photo column 470×1350 (ask 9:16), report cover hero 5.84×2.05in and `accent:` band 7.0×2.2in (ask 16:9) — all `object-fit:cover` so everything is centre-cropped, no Gemini ratio matches exactly, and the survival rules are compose-for-the-centre-band plus steer-with-`imagePos`-after-looking-at-the-render; (4) **the imagery brief is a standing step**, not a conditional one — after synthesis and before any build, write `markets/<m>/collateral/image-brief.md` with one block per image (file · slot · imagePos · prompt) carrying the learned prompt bans (no text/people/logos/charts, no baked-in vignette/edge-fade/gradient/shadow, uniform flat background to all four edges, named Ledger hexes, stated aspect ratio, stated composition margin). Real-photos-only still governs: generated illustration is fine, a photograph implying something happened is not, and never an AI photo of Paul. init-workspace copies it into the workspace beside CLAUDE.md and PLAYBOOK.md. **Paired with `content/studio/DESIGN.md` since 2026-07-30 — FORMATS.md is the container, DESIGN.md is the look; neither repeats the other.** |
| content/studio/DESIGN.md | THE COLLATERAL DESIGN LANGUAGE (2026-07-30, Paul: Cowork "seems as though it's still drifting and wanting to reference design languages from last year" even with FORMATS.md in the workspace). **FORMATS.md was necessary and not sufficient**: it says what shape the container is, and nothing that reached the workspace said what the house LOOKS like. Verified rather than assumed — `workspace-CLAUDE.md` and `PLAYBOOK.md` carry **zero** hexes and zero typefaces between them, FORMATS.md's four are inside a Gemini-prompt example, and `DESIGN_LANGUAGE.md` lives at the repo root and is **not** in `init-workspace`'s `law()` list, so it never travelled. A session therefore had the slot table and no visual system, and filled the gap from familiarity with a repo that carries **seven retired palettes in committed CSS** — the tell was always a warm accent where a green belongs. So this file carries: a paste-block summary, **the dead-systems table with hexes** (the actual antidote — a session cannot catch itself without knowing `#FF385C`/`#D4714E`/`#185ABD` are retired), the `--pd-coral`-is-green variable-name trap, the palette and type ladder, the **page-by-page layout grammar of all three builders** derived from `house/deck.ts` + the builder CSS rather than memory, the standing marks (logo, headshot, byline), graphics doctrine, and a drift checklist whose first item is "you wrote HTML or CSS". Deliberately does NOT restate FORMATS.md's slot dimensions — two copies of a measurement is how one goes stale. **`npm run test:design` (48 cases) is what keeps it from becoming a second source of truth**: every hex must be a real `house/tokens.ts` token, every token must appear in the §4 tables, no retired hex may appear outside the dead table or the ban clause, and the LIVE SITE stylesheet is checked against the same tokens — so "the collateral looks like the website" is a checked fact, not a claim. Three of those cases exist because the first version passed a mutated file clean (a retired hex swapped into the paste block escaped both a §3-onward scope and a per-paragraph scope; the exemption now runs from the `Never:` marker to end of paragraph). init-workspace copies it in as `DESIGN.md`. |
| content/studio/posting-plan.md | The local posting plan (replaces the app campaign scheduler): format vocabulary, THE LINE laws, one entry per slot with `status: next\|drafting\|posted\|parked`. A Cowork session reads the `next` slot, verifies numbers, builds the deck. init-workspace copies it into the workspace. |
| STUDIO_COWORK.md | THE RUNBOOK for rule 0 — run the whole PRACTICE locally (rewritten 2026-07-27; it previously described a deck-only content engine). Layout (repo = engine · `~/Documents/smbx-studio` = system of record, carrying its own CLAUDE.md + PLAYBOOK.md), one-time setup, **the four jobs** (fold research into a master · derive a corp-dev document · produce collateral · deal analysis) with the audit/thesis/builder commands for each, the two traps named (a target map built from a market master invents companies; a thesis silently goes stale the moment its master is re-synthesized), the LinkedIn per-slot loop, image resolution + `assets.mts`, the honest audit caveat (numbers, not prose), and the Drive fallback for remote sessions only. Also corrects a claim that had gone false: `studio-kit/` is a **deck builder only** — no report/one-pager builder, no audit, no thesis register, older init-workspace — so the practice uses the repo, not the kit. |
| client/src/components/v6/desktop/screens/StudioAnnouncement.tsx | Studio Media + Announcement section (mounted in StudioResearch): photo upload, focal sliders, announcement copy form, render/download/preview |
| client/src/components/v6/desktop/screens/StudioResearch.tsx | Studio "Research & campaigns" mode — Paul's control panel, FORMAT-FIRST (2026-07-18): the primary picker is his weekly posting plan's own vocabulary (Teardown · Contrarian Take · How Buyers Think · Practitioner Note · Human Thread · Hand-Raiser = POST_ANGLES), the research lens auto-matches via FORMAT_LENS and stays overridable; then topic/depth/output + cadence campaigns + draft→review→approve. The screen is a CANVAS APP (2026-07-18, Paul: "full screen / canvas app… use the canvas to make Studio a great campaign manager"; supersedes the same-day two-column workspace): StudioCreate research mode renders a fixed full-viewport frame (no page scroll) — the CAMPAIGN MANAGER owns the whole content area: sidebar ("+ New research" pill · "Import a plan" — a Claude-written strategy PDF or pasted text goes to the API as a native document block via parseCampaignPlan (researchAgent.ts, POST /research/import-plan), comes back as proposed campaigns in the app vocabulary, review-then-create · LIBRARY All research / One-off runs / Archived · one folder per campaign — clicking it opens the CAMPAIGN INSPECTOR (rename, standing mandate, format/cadence edit, Run now/Pause/Archive/Delete; campaign archive = migration 104 `archived`, scheduler skips archived, sidebar Archived-campaigns group) · ASSETS Media + Collateral; panes are RESIZABLE (drag dividers, double-click to fit, widths persist; sidebar auto-fits the longest campaign name on first load)), columned file list with folder-contextual toolbar (Media → Upload photo + Focal points; Collateral → New card), and a 400px INSPECTOR that hosts the CREATION FORM (via "+ New research" or empty selection) or the selected item — run documents + Claude-style live activity feed (3s poll) + manager verbs (Run again · Archive · Delete · file-under-campaign select via PATCH /research/runs/:id, migration 103 `archived`), or asset preview (authed img + Download/Delete). The composers (StudioAnnouncement + StudioPostCards) and the ReviewPanel open as right slide-over SHEETS inside the frame (absolute, never fixed — Safari rule). Media photos AND rendered collateral live in studio_assets (`kind` photo|collateral, migration 103; run_id/schedule_id provenance, migration 105); every announcement/post-card render lands in the Collateral folder (save:true), every review-sheet/document export (1-pager PNG, carousel PDF, report PDF via ?save=1) files itself into Collateral linked to its run+campaign, campaign folders list their collateral after their runs, and the composer photo pickers filter collateral out. The review sheet renders the ACTUAL 1-pager preview (visible loading state, one auto-retry, error + Try again, click-to-full-size). FULL FINDER (2026-07-19, Paul: "make everything in the Studio drag and droppable… campaign folders too"): every row is DRAGGABLE — runs and collateral drop onto campaign folders (PATCH scheduleId), onto One-off runs/Collateral to unfile, runs onto Archived to archive; campaigns themselves drag into CAMPAIGN GROUPS (migration 106 `research_schedules.folder`; GroupHead drop targets; "+ Group" makes a pending group that persists when a campaign lands in it; groups render as indented sidebar sections). Payloads travel as dataTransfer application/json {t:"run"|"asset"|"camp", id}; SideRow/GroupHead show a hot highlight on dragover. PDFs preview INLINE in the inspector (`<object type="application/pdf">`, blob-fetched with auth — no more "use Download"); a complete one-off run offers "Make a campaign" (prompt name → POST schedules runNow:false → run filed under it); inspector resizes to 680px max. PERFORMANCE section (2026-07-19): "LinkedIn analytics" folder — Import LinkedIn export (.xlsx) toolbar button, rows with period/Analyzed·Analyzing·Failed·Imported status, inspector shows the import's VERBATIM stat grid + top posts + "Analyze with Yulia" → Yulia's read rendered in-pane (MdLite house-markdown renderer) with Download (.md)/Re-analyze; see server/services/linkedinAnalytics.ts. |
| server/services/tools.ts | 35 agentic tools |
| server/services/definitiveConnectorDistribution.ts | Claude-first connector launch package and marketplace evidence pack |
| server/services/definitiveAssistantDistributionReadiness.ts | Claude/ChatGPT/MCP launch readiness and revenue blocker map |
| server/services/definitiveOpenApiSpec.ts | OpenAPI package for ChatGPT GPT Actions and enterprise review |
| server/services/definitiveRemoteMcpTransport.ts | Streamable HTTP `/mcp` transport and registry `server.json` surface |
| server/services/definitiveMcpAuthMetadata.ts | OAuth protected-resource metadata and `WWW-Authenticate` challenge helper for `/mcp` |
| server/services/premiumPdfRenderer.ts | Puppeteer HTML→PDF with Chart.js |
| server/services/chartService.ts | Chart configs for PDF export |
| server/services/subscriptionService.ts | Subscription management + Stripe |
| server/templates/pdf/ | HTML templates for premium PDF export |
| server/worker.ts | pg-boss scheduled jobs |
| Dockerfile | Node 22 Alpine + Chromium + fonts |

## Dead Code (deleted — do not recreate)
- walletService.ts, paywallService.ts, dealExecutionFee.ts, platformFeeService.ts
- Chat.tsx, Home.tsx, Advisors.tsx, Buy.tsx, Sell.tsx (standalone pages)
- chat/Sidebar.tsx, HomeSidebar.tsx, PublicChatInput.tsx (orphaned)
- chartjs-node-canvas (native canvas dep removed — charts render in Puppeteer page)
- All old logo files (x-logo.png, X2 Transaparant.png, GX.png, redx.png, etc.)
- 2026-07-10 pre-retool demolition: components/chat/ (except ChatMorph.tsx), components/mobile/ (except StarterSheet.tsx), components/content/, components/canvas/, components/artifacts/, v6/Learn.tsx, v6/workspace.css (live .wkseal/.wk-tick rules extracted to v6/shared/workseal.css), shared/ChapterStrip|DotField|Reveal, lib/wkTheme.ts, lib/randomTextures.ts, v6/shared/verdictMaterial.ts; public/textures pruned to texture-hero-1/2 (+ png masters)

## AI Orchestration
| Task | Engine |
|------|--------|
| Chat/Conversation | Claude Sonnet 4.6 |
| Agentic Loop | Claude Sonnet 4.6 (35 tools, 10-round limit) |
| Deliverable Generation | Tier-routed (28 generators) |
| CIM Generation | Claude Opus 4.6 |
| Intelligence Brief | Claude Sonnet 4.6 (sourcing Stage 1) |
| Website Enrichment | Claude Haiku 4.5 |
| Gate Summarization | Claude Haiku 4.5 |
| Internal Research Agent | Claude Sonnet 4.6 + server web_search/web_fetch (20260209 tools) |

## Commands
```bash
npm run dev          # Start Vite dev server (frontend only, port 5173)
npx tsx server/index.ts  # Start Express backend (port 3000)
npm run build        # Build for production
```

## Environment Variables
DATABASE_URL, ANTHROPIC_API_KEY, GOOGLE_AI_API_KEY, OPENAI_API_KEY,
GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_PLACES_API_KEY,
JWT_SECRET, NODE_ENV, PORT, APP_URL, CENSUS_API_KEY, TEST_MODE,
PRACTICE_MODE (default true), TEAM_ALLOWLIST (comma-separated emails),
ENGAGED_LANES (comma-separated keyword groups, e.g. "hvac phoenix, plumbing dallas" —
  the honest register behind Yulia's "this lane is open" close; empty = all lanes open),
RESEND_API_KEY + EMAIL_FROM (lead pings + transactional email; console-log fallback),
RESEARCH_MONTHLY_CAP_CENTS (research agent monthly budget — UNSET/0 = NO LIMIT, the default
  since 2026-07-21, Paul: "from the app perspective let's remove any limits" after the old
  $150 default blocked his month; set a positive cents value to opt back into the gate),
RESEARCH_SCHEDULES_DISABLED (set "true" to stop the in-process campaign scheduler),
RESEARCH_DECK_MODEL (deck-designer model, default claude-sonnet-4-6) and
RESEARCH_DECK_DESIGNER (set "off" to force the fixed template — Claude designs by default),
RESEARCH_ARTWORK_AUTOGEN (set "true" to let runs auto-call the Gemini image API at
  completion/first-download; DEFAULT OFF — Paul 2026-07-20: he generates images in
  the Gemini app, uploads to Media, picks in the review sheet; the explicit
  Generate buttons work regardless of this flag),
STRIPE_* (dormant: SECRET_KEY, PUBLISHABLE_KEY, WEBHOOK_SECRET, PRICE_SOLO/PRO/TEAM/ENTERPRISE)
