services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: smbx
      POSTGRES_USER: smbx
      POSTGRES_PASSWORD: smbx
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
