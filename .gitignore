node_modules
dist
dist-preview
.env
test-results/
playwright-report/
testing/v6-desktop-smoke/latest/
testing/v6-mobile-smoke/latest/
uploads/
# Texture masters moved out of public/ (2026-06-11 ship-weight fix) — local archive only
client/textures_backup/
.DS_Store
.claude/settings.local.json
.claude/worktrees/

# Generated test-run artifacts (not source)
testing/agent-pov/results/
testing/definitive/results/

# stray local sqlite (app uses Railway Postgres)
database.db
fold-shots/
# `npm run shoot:mobile` strip captures — 200+ PNGs (~43MB) regenerated on
# every audit run. The FINDINGS are the artifact and they live in the run
# output; the strips are what you look at while fixing one.
mobile-shots/
